# 开发排期与提交流程

## 1. 文档定位

本文档用于固定 `home-service-payment-system` 的开发排期、模块推进顺序和 Git 提交节奏。

生效日期：

- `2026-07-19`

## 2. 执行原则

从 `2026-07-19` 开始，项目按以下顺序推进：

1. 先排期
2. 再做需求与设计
3. 再做代码实现
4. 最后做 Git 提交与 GitHub 推送

后续不允许脱离排期零散开发。

## 3. 总排期

### 3.1 第一阶段：支付核心系统一期 MVP

时间范围：

- `2026-07-19` 至 `2026-07-26`

目标：

- 完成 `systems/payment-core` 的一期可开发版本
- 完成数据库设计、前端设计、后端设计、接口文档、代码实现
- 打通 admin-web、后端接口、MySQL

阶段任务：

1. `2026-07-19`
   - 项目初始化
   - Git 建仓
   - GitHub 绑定
   - 系统目录拆分
2. `2026-07-19` 至 `2026-07-20`
   - 补齐 payment-core 文档
   - 拆分前端端结构
   - 确认数据库和运行环境
3. `2026-07-20` 至 `2026-07-22`
   - 接入 MySQL
   - 完成建表、初始化数据
   - 完成后端统一返回体和查询接口改造
4. `2026-07-22` 至 `2026-07-24`
   - admin-web 适配新接口
   - 补齐加载态、错误态、空态
   - 收敛后台页面结构
5. `2026-07-24` 至 `2026-07-26`
   - 联调
   - 修复问题
   - 输出一期阶段总结

### 3.2 第二阶段：账户账务与清结算

计划时间：

- `2026-07-27` 至 `2026-08-09`

目标：

- 新增 `accounting-system`
- 新增 `clearing-system`
- 建立账户、流水、清分、结算能力

### 3.3 第三阶段：退款中心、对账、保证金

计划时间：

- `2026-08-10` 至 `2026-08-23`

目标：

- 新增 `refund-center`
- 新增 `reconciliation-system`
- 新增 `deposit-system`

### 3.4 第四阶段：钱包、营销、线下汇入、业财协同

计划时间：

- `2026-08-24` 之后

目标：

- 完成完整资金平台扩展能力

### 3.5 2026-08-06 滚动复核与当前优先级

基于当前仓库真实代码量、测试量、联调证据和门禁材料，当前排期不再只是最初纸面计划，而需要按实际成熟度重排：

1. 已进入“可冻结交付收尾”阶段的系统：
   - `payment-core`
2. 已形成较完整后端主链路、但仍需继续产品化和门禁增强的系统：
   - `accounting-system`
   - `clearing-system`
   - `settlement-system`
   - `wallet-system`
   - `wallet-account`
3. 当前实现和测试深度明显偏薄、应优先进入下一轮自动化迭代的系统：
   - `deposit-system`
   - `reconciliation-system`
   - `refund-center`
   - `risk-control-system`
   - `ops-config-system`
   - `gateway-access`

当前工程量快照（截至 `2026-08-06`）：

| 系统 | Java/XML 文件数 | 前端文件数 | 测试数 | 当前判断 |
| --- | --- | --- | --- | --- |
| `payment-core` | `325` | `1821` | `56` | 进入冻结证据收尾 |
| `accounting-system` | `70` | `454` | `12` | 已有较完整实现，可继续补强 |
| `clearing-system` | `61` | `453` | `12` | 已有较完整实现，可继续补强 |
| `settlement-system` | `54` | `453` | `8` | 已有较完整实现，可继续补强 |
| `wallet-system` | `54` | `449` | `5` | 中等成熟度，需继续产品化 |
| `wallet-account` | `34` | `448` | `2` | 中等成熟度，需继续门禁增强 |
| `deposit-system` | `18` | `397` | `1` | 下一批优先补齐 |
| `reconciliation-system` | `24` | `397` | `1` | 下一批优先补齐 |
| `refund-center` | `27` | `397` | `2` | 下一批优先补齐 |
| `risk-control-system` | `35` | `397` | `1` | 下一批优先补齐 |
| `ops-config-system` | `32` | `397` | `1` | 下一批优先补齐 |
| `gateway-access` | `28` | `397` | `2` | 下一批优先补齐 |

因此，当前默认推进顺序调整为：

1. 先完成 `payment-core` 冻结版证据与发布门禁收尾
2. 再优先补齐三期核心系统：
   - `refund-center`
   - `reconciliation-system`
   - `deposit-system`
3. 同步增强支撑域：
   - `risk-control-system`
   - `ops-config-system`
   - `gateway-access`
4. 最后再推进更大范围的钱包、营销、线下汇入和业财协同扩展

## 4. 模块开发顺序

每个模块都必须按如下顺序执行：

1. 确认排期
2. 明确模块范围
3. 输出数据库设计
4. 输出前端页面设计
5. 输出后端详细设计
6. 输出接口文档
7. 开始代码开发
8. 做本地验证
9. 提交 Git
10. 推送 GitHub

## 5. Git 提交节奏

后续不采用“大改一堆最后一次性提交”的方式，而采用小步提交。

建议提交粒度：

1. 文档基线提交
2. 目录结构提交
3. 数据库设计提交
4. 后端接口提交
5. 前端页面提交
6. 联调修复提交

提交信息建议：

- `docs: ...`
- `feat: ...`
- `refactor: ...`
- `fix: ...`
- `chore: ...`

## 6. Git 分支与发布流程

从 `Sunday, July 19, 2026` 开始，项目采用以下分支策略：

### 6.1 分支职责

- `test`
  - 日常开发、自测、联调默认在该分支推进
  - 新需求、新系统、新改造先进入 `test`
- `master`
  - 只承接已经完成自测并确认可发布的代码
  - 作为稳定主干
- `release/*`
  - 在需要发版时，从 `master` 按版本号切出发布分支
  - 用于版本冻结、最终检查和打包

### 6.2 分支命名规范

- 正常需求：
  - `feature/需求名`
- 缺陷修复：
  - `hotfix/需求名-缺陷`
- 发布分支：
  - `release/版本号`

示例：

- `feature/payment-core-phase-b`
- `feature/accounting-system-v1`
- `hotfix/payment-core-callback-dup-fix`
- `release/v1.0.0`

### 6.3 标准流程

标准流程必须是：

1. 需求设计与代码开发先进入 `test`
2. 具体开发在 `feature/*` 或 `hotfix/*` 分支进行
3. 完成后先合入 `test`
4. 在 `test` 完成自测
5. 自测通过后再进入 `master`
6. 基于 `master` 在某个时刻切 `release/*`
7. 在 `release/*` 上完成发布包和版本号管理

### 6.4 当前执行要求

后续我在仓库中的默认工作方式是：

1. 先按排期做文档设计
2. 再从 `test` 拉出对应的 `feature/*` 或 `hotfix/*`
3. 在该分支推进代码
4. 完成本地验证和自测
5. 先合入 `test`
6. 自测通过后再推进到 `master`
7. 发布时再配合生成 `release/*` 版本

## 7. 当前约束

当前 `payment-core` 的开发也必须服从这份排期文档。

也就是说，后续需求开发、设计、实现和 Git 提交，都以本文件为基准执行。

## 8. 持续优化规则

每完成一轮设计、开发或测试后，都要额外做一次对标检查：

1. 对照市面同类产品
2. 对照当前母稿文档
3. 判断是否有更优实现
4. 如果有，则先加入对应系统的需求列表
5. 再基于需求列表继续完成设计、开发、测试和提交

要求：

- 不是发现更优实现后直接口头记住
- 而是必须先进入系统需求列表
- 再按列表进入下一轮排期和开发
