# 集成交付门禁审计

## 1. 审计目的

本记录用于判断当前整包增量能否从 `feature/payment-core-phase-b` 进入 `test`，并作为后续 `test -> master -> release/*` 的审计依据。

当前分支不是单一支付核心改动，已同时包含：

1. `payment-core`
2. `wallet-system`
3. `gateway-access`
4. `clearing-system`
5. `settlement-system`
6. `accounting-system`

## 2. 已完成门禁

### 2.1 后端自动化测试

| 系统 | 已验证结果 | 结论 |
| --- | --- | --- |
| `payment-core` | `171` 个测试通过，`0` failures，`0` errors | 通过 |
| `wallet-system` | `5` 个核心资金动作测试通过，`0` failures，`0` errors | 通过 |
| `gateway-access` | `8` 个测试通过，`0` failures，`0` errors | 通过 |
| `clearing-system` | `3` 个测试通过，`0` failures，`0` errors | 通过 |
| `settlement-system` | `3` 个测试通过，`0` failures，`0` errors | 通过 |
| `accounting-system` | `3` 个测试通过，`0` failures，`0` errors | 通过 |

统一执行环境：

```text
JDK: /Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home
Maven: /Users/abc123/apache-maven-3.9.16/bin/mvn
Repository: /Users/abc123/apache-maven-3.9.16/repository
```

### 2.2 前端生产构建

| 系统/端 | 结果 |
| --- | --- |
| `payment-core/admin-web` | 通过 |
| `payment-core/app-web` | 通过 |
| `payment-core/h5-web` | 通过 |
| `payment-core/pc-web` | 通过 |
| `wallet-system/admin-web` | 通过 |
| `gateway-access/admin-web` | 通过 |
| `clearing-system/admin-web` | 通过 |
| `settlement-system/admin-web` | 通过 |
| `accounting-system/admin-web` | 通过 |

### 2.3 工程规范与证据

1. 本轮改动已执行 `git diff --check`。
2. 钱包系统已补 `WalletCoreFundsServiceImplTest`，不再是“命令成功但零测试”的假通过状态。
3. `payment-core`、`wallet-system` 已回填自测记录。

## 3. 分支判定

### 3.1 `feature/* -> test`

当前允许进入 `test`，依据如下：

1. 六个系统的后端自动化测试均已通过。
2. 九个前端构建目标均已通过。
3. 各系统已具备本轮自测记录，跨系统 smoke 结果已沉淀到《跨系统联调与 Smoke 测试记录》。
4. 已验证 `支付成功事件 -> 清分 -> 结算 -> 账务入账` 的事件契约和消费结果。
5. 本轮改动已执行 `git diff --check`。

### 3.2 `test -> master / release`

当前仍禁止进入 `master` 或创建 `release/*`，原因如下：

1. 跨系统 smoke 当前使用显式事件投递，尚未验证生产级 MQ 自动发布、自动消费、重试、死信和补偿。
2. 尚未完成 `test` 分支上的整包复验和稳定观察。
3. 整包数据库建库建表、跨系统失败补偿和真实支付成功事件自动投递仍需继续验证。

## 4. 下一步门禁计划

1. 合入 `test`，在 `test` 分支重新执行全量构建、后端测试和跨系统 smoke。
2. 补支付成功事件自动投递到清分系统的适配能力，并覆盖失败重试、幂等、死信和补偿。
3. 执行整包数据库初始化与关键链路 smoke test。
4. `test` 稳定后再评估 `master` 合并与 `release/*` 发布文档。

## 5. test 分支复验结果

当前 `feature/payment-core-phase-b` 已合入 `test`，并完成首轮稳定性复验：

| 项目 | 结果 |
| --- | --- |
| `payment-core` 后端自动化测试 | `171` 个测试通过，`0` failures，`0` errors |
| `payment-core/admin-web` 构建 | `npm run build` 通过 |
| `test` 分支工作区状态 | 干净，无未提交改动 |

当前判断：

1. `test` 分支已具备继续观察和补门禁的基础。
2. 仍未满足 `master` / `release` 条件，原因不再是“feature 未验证”，而是“test 稳定观察、自动事件投递、数据库和补偿门禁尚未完成”。

## 6. 2026-07-29 新增门禁证据

### 6.1 payment-core 后端回归

| 项目 | 命令/方式 | 结果 |
| --- | --- | --- |
| `payment-core` 全量测试 | `JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home /Users/abc123/apache-maven-3.9.16/bin/mvn -Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository test` | `171` 个测试通过，`0` failures，`0` errors，`0` skipped |

### 6.2 payment-core 自动下游 smoke

| 项目 | 业务编号 | 结果 |
| --- | --- | --- |
| 预付单创建 | `SMOKE-ORDER-20260729-003` | 成功 |
| 支付成功回调 | `PAY1785287995355` | 支付单状态 `SUCCESS` |
| 清分消费验证 | `PAY1785287995355` | `EVT60003` 已消费 |
| 账务消费验证 | `PAY1785287995355` | `EVT50003` 已消费 |

### 6.3 审计判断

1. `payment-core` 已从“只验证单测 + 人工补投事件”提升到“真实支付回调后自动下发清分、账务事件”的阶段。
2. 当前仍不能进入 `master` 或 `release/*`，因为 `settlement-system` 自动串联、消息中间件级可靠投递、补偿/死信/压测证据仍未完成。

## 7. 2026-07-29 最新复核结论

### 7.1 最新后端统一回归

| 系统 | 命令/方式 | 结果 |
| --- | --- | --- |
| `payment-core` | `JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home /Users/abc123/apache-maven-3.9.16/bin/mvn -Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository -f /Users/abc123/workspace/home-service-payment-system/systems/payment-core/backend/pom.xml test` | `171` 个测试通过，`0` failures，`0` errors，`0` skipped |
| `wallet-system` | `... -f /Users/abc123/workspace/home-service-payment-system/systems/wallet-system/backend/pom.xml test` | `5` 个测试通过，`0` failures，`0` errors，`0` skipped |
| `gateway-access` | `... -f /Users/abc123/workspace/home-service-payment-system/systems/gateway-access/backend/pom.xml test` | `8` 个测试通过，`0` failures，`0` errors，`0` skipped |
| `clearing-system` | `... -f /Users/abc123/workspace/home-service-payment-system/systems/clearing-system/backend/pom.xml test` | `5` 个测试通过，`0` failures，`0` errors，`0` skipped |
| `settlement-system` | `... -f /Users/abc123/workspace/home-service-payment-system/systems/settlement-system/backend/pom.xml test` | `5` 个测试通过，`0` failures，`0` errors，`0` skipped |
| `accounting-system` | `... -f /Users/abc123/workspace/home-service-payment-system/systems/accounting-system/backend/pom.xml test` | `3` 个测试通过，`0` failures，`0` errors，`0` skipped |

### 7.2 当前分支门禁判断

1. `test` 分支当前已经具备继续迭代和持续补门禁的稳定基础，至少在六个核心后端子系统维度都不存在“最新代码未回归”的问题。
2. 当前仍不自动推进 `test -> master`，因为以下门禁证据仍不足：
   - 缺少 MQ 级可靠投递、失败重试、死信补偿、整包建库建表与回滚演练证据
3. 因此当前正确动作仍是继续在 `test` 分支补强证据链，而不是提前合并到 `master` 或创建 `release/*`。

## 8. 2026-07-29 最新前端统一回归

### 8.1 前端统一构建结果

| 系统/端 | 命令/方式 | 结果 |
| --- | --- | --- |
| `payment-core/admin-web` | `npm run build` | 通过 |
| `payment-core/app-web` | `npm run build` | 通过 |
| `payment-core/h5-web` | `npm run build` | 通过 |
| `payment-core/pc-web` | `npm run build` | 通过 |
| `wallet-system/admin-web` | `npm run build` | 通过 |
| `gateway-access/admin-web` | `npm run build` | 通过 |
| `clearing-system/admin-web` | `npm run build` | 通过 |
| `settlement-system/admin-web` | `npm run build` | 通过 |
| `accounting-system/admin-web` | `npm run build` | 通过 |

### 8.2 当前整包门禁判断

1. 到目前为止，`test` 分支已经同时具备六个核心后端子系统的最新测试通过证据，以及九个前端构建目标的最新生产构建通过证据。
2. 这意味着“整包最新代码没有统一前后端回归”已经不再是阻断 `master / release` 的理由。
3. 当前剩余阻断项进一步收敛为三类：
   - MQ 级可靠投递、重试、死信和补偿机制仍缺生产级验证
   - 整包数据库初始化、回滚说明和发布后验证清单还缺最新演练证据
   - `test` 分支虽已连续收口，但还缺一版可直接用于 `release/*` 的正式发布文档
4. 因此现阶段仍继续在 `test` 上推进，不提前合并到 `master`。

## 9. 2026-07-29 最新数据库初始化演练

### 9.1 演练结果

| 系统 | 验证方式 | 结果 |
| --- | --- | --- |
| `payment-core` | 查询现有库 `housekeeping_payment_core` 的 `t_payment_order / t_payment_channel_config` 与演示数据 | 通过，支付渠道 `3` 条，支付单 `7` 条 |
| `wallet-system` | 执行 `schema.sql + data.sql` 后查询 `t_wallet_account / t_wallet_ledger` | 通过，钱包账户 `3` 条，钱包流水 `3` 条 |
| `gateway-access` | 执行 `schema.sql` 后查询 `t_gateway_app / t_gateway_permission` | 通过 |
| `clearing-system` | 执行 `schema.sql` 后查询 `t_clearing_batch / t_clearing_order` | 通过 |
| `settlement-system` | 执行 `schema.sql` 后查询 `t_settlement_batch / t_payout_batch` | 通过 |
| `accounting-system` | 执行 `schema.sql` 后查询 `t_account / t_account_ledger` | 通过 |

### 9.2 当前门禁判断

1. “整包数据库初始化、回滚说明和表级验证”已经不再只是文档计划，而是具备了真实 MySQL 执行证据。
2. 当前数据库门禁剩余缺口主要收敛到两类：
   - 还缺正式发布版本的数据库变更清单与发布后验证清单
   - 还缺 MQ 级可靠投递、失败重试、死信和补偿的生产级验证
3. 因此当前仍不推进 `test -> master`，但数据库交付这一类阻断项已显著缩小。

## 10. 2026-07-31 payment-core 导出闭环复核

### 10.1 payment-core 最新后端回归

| 项目 | 命令/方式 | 结果 |
| --- | --- | --- |
| `payment-core` 全量测试 | `JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home /Users/abc123/apache-maven-3.9.16/bin/mvn -Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository -f /Users/abc123/workspace/home-service-payment-system/systems/payment-core/backend/pom.xml test` | `196` 个测试通过，`0` failures，`0` errors，`0` skipped |

### 10.2 四块导出闭环证据

| 模块 | 接口 | 导出文件 | 当前证据 |
| --- | --- | --- | --- |
| `bills` | `GET /api/bills/export` | `bills.csv` | `BillService/BillServiceImpl/BillController/BillMapper/BillMapper.xml` 已闭环，`BillServiceImplTest`、`BillControllerTest` 已覆盖，接口文档与自测记录已回填 |
| `cashier-sessions` | `GET /api/cashier-sessions/export` | `cashier-sessions.csv` | `CashierSessionService/CashierSessionServiceImpl/CashierSessionController/CashierSessionMapper/CashierSessionMapper.xml` 已闭环，`CashierSessionServiceImplTest`、`CashierSessionControllerTest` 已覆盖，接口文档与自测记录已回填 |
| `refunds` | `GET /api/refunds/export` | `refunds.csv` | `RefundService/RefundServiceImpl/RefundController/RefundMapper/RefundMapper.xml` 已闭环，`RefundServiceImplTest`、`RefundControllerTest` 已覆盖，接口文档与自测记录已回填 |
| `payment-flows` | `GET /api/payment-flows/export` | `payment-flows.csv` | `PaymentFlowService/PaymentFlowServiceImpl/PaymentFlowController/PaymentFlowMapper/PaymentFlowMapper.xml` 已闭环，`PaymentFlowServiceImplTest`、`PaymentFlowControllerTest` 已覆盖，接口文档与自测记录已回填 |

补充说明：

## 11. 2026-08-03 隔离 MQ 主链路复核

### 11.1 本轮新增运行证据

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| 隔离 RabbitMQ 拓扑 | `infra/rabbitmq/declare_isolated_topology.sh 20260803a` | 生成 `payment.success / clearing.generated` 对应的主队列、retry、DLQ 隔离拓扑 |
| 隔离服务进程 | `payment-core:18081`、`clearing:18122`、`settlement:18132`、`accounting:18112` | 四个服务均监听成功 |
| 支付主链路 | `DRILL-AMQP-20260803A-002` | 生成 `PRE1785733720393 / PAY1785733720390 / BILL1785733720388`，支付成功收口 |
| 清分事实 | `GET /api/clearing/orders` | 生成 `CLO20002` |
| 结算事实 | `GET /api/settlements/orders` | 生成 `SLT20003` |
| 账务事实 | `GET /api/accounting/events` + `GET /api/accounting/balances/ACT10003` | `EVT50002 / EVT50003` 已消费，平台账户余额明细可查 |
| 队列快照 | `infra/rabbitmq/queue_snapshot.sh 20260803a` | 截至 `2026-08-03 13:09:42 +0800`，隔离主队列、retry、DLQ 均无积压 |

### 11.2 本轮审计判断

1. 之前“尚未验证生产级 MQ 自动发布、自动消费”的阻断项，至少在本地隔离 RabbitMQ 维度已经拿到了新一轮真实运行证据。
2. 当前可以明确判定：`payment-core` 不只是单体成功，而是已经再次验证 `PAYMENT_SUCCESS -> CLEARING_GENERATED -> settlement/accounting` 的真实异步链路。
3. 但这仍不足以触发 `test -> master` 或 `release/*`，原因如下：
   - 本轮未同步完成同批次 retry、DLQ、人工结案、定向 replay 的完整补偿证据
   - 本轮未同步完成 `settlement-system` 出款执行链路的同批次闭环
   - 前端四端页面矩阵与这条最新隔离主链路之间还缺最新一轮全链路人工回归记录

### 11.3 当前分支结论

1. `test` 分支继续推进是合理的，且 `payment-core` 可以作为后续其他系统联调的稳定上游基线。
2. 当前仍不自动合并 `master`，也不自动创建新的 `release/*`。
3. 下一轮优先级应聚焦：
   - 同一隔离批次补齐 retry / DLQ / replay / 人工结案
   - 同一隔离批次补齐结算出款执行链路
   - 补前端四端围绕真实支付主链路的统一回归证据

## 12. 2026-08-03 结算出款闭环与接口异常正式化

## 13. 2026-08-04 运营配置域生效链路门禁补强

### 13.1 本轮新增证据

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| `ops-config-system` 有效配置快照接口 | `GET /api/ops-config/effective-snapshot?businessCode=HOME_CLEAN&payType=PAY_CONSUME&terminalType=APP` | 可返回默认支付方式、优先/备选渠道和启用控制项 |
| `ops-config-system` 后端测试 | `mvn test` | `4` 个测试通过，`0` failures，`0` errors，`0` skipped |
| `payment-core` 提交支付定向测试 | `mvn -Dtest=PaymentServiceImplTest test` | `26` 个测试通过，覆盖默认支付方式、默认渠道自动补齐 |
| `payment-core` 全量后端回归 | `mvn test` | `263` 个测试通过，`0` failures，`0` errors，`0` skipped |

### 13.2 本轮审计判断

1. `payment-core` 已不再只依赖本地静态默认值，而是能在支付提交主链路中真实消费 `ops-config-system` 的有效配置快照。
2. 这意味着支付核心、运营配置域、独立风控域之间的分层边界更清晰：
   - 运营配置域负责默认支付方式、路由基线和系统控制项
   - 支付核心负责交易编排和状态收口
   - 风控域负责提交前准入判断
3. 当前仍不能据此推进 `test -> master`，因为以下证据仍不完整：
   - 跨进程真实 HTTP 联动 smoke 还需补 `ops-config-system + risk-control-system + payment-core` 同批次运行证据

## 14. 2026-08-05 正式来源应用跨系统门禁补强

### 14.1 本轮新增证据

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| 正式来源应用控制策略复核 | `GET /api/payment-configs/control-policies` | `housekeeping-app-web`、`housekeeping-h5-web`、`housekeeping-pc-web` 三个正式来源应用控制策略均为 `PASS` |
| 正式来源应用支付主链路 | `POST /api/payments/prepay` + `POST /api/payments/submit` + 渠道回调 | App `PAY1785918393972`、H5 `PAY1785918388803`、PC `PAY1785918388713` 三笔样例全部收口为 `SUCCESS` |
| 正式来源应用幂等/关单守卫 | 重复 `SUCCESS` 回调 + `POST /api/payments/close` | 三笔 `SUCCESS` 支付单重复回调后仍保持 `SUCCESS`，显式关单不会把成功单错误改为 `CLOSED` |
| 清分自动联动复核 | `GET /api/clearing/orders` | 生成 `CLO20003 / CLO20004 / CLO20005`，三笔正式来源应用支付样例全部自动生成清分单，状态均为“清分成功” |
| 结算自动联动复核 | `GET /api/settlements/orders` | 生成 `SLT20004 / SLT20005 / SLT20006`，三笔正式来源应用支付样例全部自动生成结算单，状态均为“待审核 / 待出款” |
| 账务自动联动复核 | `GET /api/accounting/events` | `EVT50005 ~ EVT50010` 已自动消费，覆盖 `PAYMENT_SUCCESS` 与 `CLEARING_GENERATED` 两类事件 |

### 14.2 本轮审计判断

1. 目前阻断 `test -> master` 的原因，已经不再是“正式来源应用无法提交或无法联动下游”，因为正式 App/H5/PC 三端样例已经重新验证了支付主链路、守卫逻辑与跨系统自动联动。
2. 当前 `payment-core` 可以被视为后续系统继续扩展和联调的稳定上游基线，尤其是来源应用控制策略、终端别名归一化、渠道别名兼容这三类正式口径问题已完成修复并拿到运行证据。
3. 但当前仍不自动推进 `master` 或 `release/*`，剩余阻断项已经明确收敛为：
   - 缺少同批次 retry / DLQ / replay / 人工结案补偿链路证据
   - 缺少正式来源应用页面级 `SUCCESS / CLOSED / exception` 回归截图证据
   - 缺少一版基于最新运行事实的冻结版发布说明与发布后验证清单
   - 配置灰度、版本回滚、审批流和跨系统配置发布仍未生产化
   - 最新前端四端围绕这条配置生效链路的人工回归记录还未追加

### 14.3 当前结论

1. 本轮适合继续收口到 `test` 分支，作为后续跨系统配置生效和真实联调的稳定基线。
2. 现阶段仍不自动合并 `master`，也不自动创建新的 `release/*`。

## 15. 2026-08-04 ops-config + risk-control + payment-core 跨进程 smoke

### 15.1 本轮新增运行证据

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| `ops-config-system` 有效配置快照 | `GET /api/ops-config/effective-snapshot?businessCode=HOME_CLEAN&payType=PAY_CONSUME&terminalType=APP` | 返回 `defaultPayMethod=微信支付`、`primaryChannelProfileCode=CHANNEL_WX_H5` |
| `ops-config-system` 控制项生效 | 运行态启用 `CONTROL_BIG_AMOUNT_REVIEW` | 最新快照已返回该控制项 |
| `risk-control-system` 直接决策 | `POST /api/risk-control/decisions/evaluate` | `PAY-RUNTIME-SMOKE-20260804-002` 返回 `REVIEW`，`riskTag=大额支付人工复核` |
| `risk-control-system` 复核留痕 | `GET /api/risk-control/review-orders` | 生成复核单 `REVIEW-1785811212924` |
| `payment-core` 预付单创建 | `POST /api/payments/prepay` | 生成 `PRE1785811244734 / PAY1785811244732 / BILL1785811244724` |
| `payment-core` 提交支付 | `POST /api/payments/submit` | 收银台状态 `待风控复核` |
| `payment-core` 支付详情 | `GET /api/payments/PAY1785811244732` | 支付状态 `RISK_REVIEW`，方式 `微信支付`，渠道 `wx_h5` |

### 15.2 本轮审计判断

1. 之前“`ops-config-system -> risk-control-system` 的配置生效链路仍未接通”这一阻断项已经关闭。
2. 当前已能证明：
   - `payment-core` 可从运营配置域补齐默认支付方式和渠道
   - `risk-control-system` 可从运营配置域读取大额复核阈值
   - 两者在真实运行态下可串成同一条支付提交准入链路
3. 本轮同时暴露并修复了 `payment-core` 的运行态 Spring 装配缺陷：
   - `PaymentRiskControlServiceImpl`
   - `PaymentOpsConfigServiceImpl`
   - `RiskOpsConfigServiceImpl`
   已显式标注主构造器注入，运行态已恢复正常

### 15.3 当前剩余阻断项

1. 还缺人工复核通过后再次提交并继续进入渠道/回调链路的同批次运行证据。
2. 还缺这条准入链路与清分、结算、账务的同批次全链路事实。
3. 还缺前端四端围绕风控复核态的统一人工回归记录。

### 15.4 当前结论

1. `test` 分支已经具备更强的跨系统配置生效与准入联动证据，适合继续向下一轮全链路烟测推进。
2. 当前仍不自动推进 `test -> master` 或 `release/*`，因为支付成功后的整链路事实与发布门禁还未完整闭合。

### 12.1 新增运行证据

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| 结算审核通过 | `POST /api/settlements/orders/SLT20003/audit` | 通过，结算单进入 `待出款` |
| 出款草稿创建 | `POST /api/settlements/payouts` | 生成 `PBT50001`，金额 `¥153.16` |
| 出款执行 | `POST /api/settlements/payouts/PBT50001/execute` | 通过，批次状态 `已完成` |
| 出款记录核对 | `GET /api/settlements/payouts/PBT50001/records` | 生成 `POU60001`，状态 `已发放` |
| 结算单终态核对 | `GET /api/settlements/orders/SLT20003/full` | 状态收口为 `已出款 / 已发放` |
| 不存在出款批次 | `POST /api/settlements/payouts/PBT-MISSING/execute` | 返回 `SETTLEMENT-1002`，不再是 500 |
| 不存在结算单 | `GET /api/settlements/orders/SLT-MISSING/full` | 返回 `SETTLEMENT-1001`，不再是 500 |
| 不存在账务账户 | `GET /api/accounting/balances/ACT-MISSING` | 返回 `ACCOUNTING-1001`，不再是 500 |

### 12.2 审计判断

1. `settlement-system` 正向出款链路已经补齐到“审核通过 -> 出款草稿 -> 执行出款 -> 出款记录 -> 结算单终态”。
2. `settlement-system` 与 `accounting-system` 的接口边角行为已从“错编号返回 500”提升为“统一业务错误码返回”，更接近正式交付包要求。
3. 这一步提升了联调和测试的判障效率，后续看到 `SETTLEMENT-1001/1002`、`ACCOUNTING-1001` 时，可以直接判定为输入对象不存在，而不是系统异常。

## 13. 2026-08-03 隔离 MQ 死信补偿闭环

### 13.1 本轮新增实现

| 项目 | 变更 | 目的 |
| --- | --- | --- |
| `infra/rabbitmq/declare_isolated_topology.sh` | 新增隔离 `*.dlq.<suffix>.v1` 到 `payment.compensation.dlq-intake` 的绑定 | 让隔离演练也能进入补偿任务账本 |
| `PaymentDeadLetterTaskServiceImpl` | 死信路由解析从固定路由键扩展为支持隔离后缀 `.<suffix>.v1` | 让隔离任务生成正确的 replay routing key |
| `PaymentDeadLetterTaskServiceImplTest` | 新增隔离死信路由测试 | 防止后续回归破坏隔离补偿能力 |

### 13.2 本轮运行证据

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| 非法消息注入 | 向 `payment.trade / payment.success.20260803a.v1` 发布 `messageId=MSG-DRILL-20260803A-001` 的非法 JSON，`x-retry-count=3` | broker 接收成功 |
| clearing / accounting 专属 DLQ | `queue_snapshot.sh 20260803a` | `clearing.payment-success.20260803a.dlq=1`，`accounting.payment-success.20260803a.dlq=1` |
| 补偿任务生成 | `GET /api/payment-dead-letter-tasks?messageId=MSG-DRILL-20260803A-001` | 生成 `2` 条 `PENDING_REVIEW` 任务，分别归属 `clearing-system` 与 `accounting-system` |
| accounting 人工结案 | `resolve-manually` | 任务 `DLQ8a04f820ab1c414c8750` 变为 `MANUAL_RESOLVED` |
| clearing 审核后重放 | `ready-to-replay` + `replay` | 任务 `DLQd45a251d45d6483795bf` 变为 `REPLAYED`，`replayCount=1` |
| replay 路由隔离 | `queue_snapshot.sh 20260803a` | replay 后仅 `clearing.payment-success.20260803a.retry=1`，`accounting.payment-success.20260803a.retry=0`，无串扰 |

### 13.3 当前门禁判断

1. “隔离拓扑下 DLQ 无法进入补偿任务账本”的缺口已关闭。
2. “带后缀隔离死信无法定向 replay” 的缺口已关闭。
3. 当前仍未完全移除 `test -> master` 阻断项，原因是：
   - 这轮 replay 已进入 retry 队列，但尚未继续等待 TTL 回流并核对后续事实
   - 本轮只覆盖 `payment.success.*` 死信补偿，尚未补 `clearing.generated.*` 死信补偿
   - 仍需整合到更完整的发布证据包后，才能评估 `master / release`

## 14. 2026-08-03 快速 retry 回流验证

### 14.1 本轮新增实现

| 项目 | 变更 | 目的 |
| --- | --- | --- |
| `declare_isolated_topology.sh` | 支持 `RETRY_TTL_MS` 环境变量 | 在不破坏正式 5 分钟配置的前提下，支持几秒级 retry 回流验证 |
| `infra/rabbitmq/README.md` | 补充快速 retry drill 使用方式 | 让后续演练可重复 |

### 14.2 本轮运行证据

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| 快速隔离拓扑 | `RETRY_TTL_MS=2000 bash infra/rabbitmq/declare_isolated_topology.sh 20260803fast` | 已生成 2 秒 TTL 的隔离拓扑 |
| 非法支付成功消息 | 注入 `MSG-FAST-PAY-20260803-001` 到 `payment.trade / payment.success.20260803fast.v1` | broker 接收成功 |
| 初次失败 | `T0` 快照 | `clearing.payment-success.20260803fast.retry=1`，`accounting.payment-success.20260803fast.retry=1` |
| 回流后终态 | `T+7s` 快照 | 两个 retry 队列均清空，对应两个专属 DLQ 各为 `1` |
| 补偿任务 | `GET /api/payment-dead-letter-tasks?messageId=MSG-FAST-PAY-20260803-001` | 新增两条 `PENDING_REVIEW` 任务，header 中包含 `x-retry-count=3` 与 `x-death.reason=expired` |

### 14.3 当前门禁判断

1. `payment.success.*` 链路的 retry 回流证据已从“消息进 retry 队列”提升到“经历 retry TTL 回流与重复失败后重新进入专属 DLQ，并再次生成补偿任务”。
2. 当前剩余补偿门禁已进一步收敛，重点转向：
   - `clearing.generated.*` 的快速 retry 回流证据
   - 更多整包冻结版所需的前端跨端、发布前后验证和整包回滚证据

1. 四块导出后端实现均采用 UTF-8 BOM 返回 CSV，并在控制器侧设置 `attachment` 文件名。
2. `payment-core` 自测记录已补四块导出闭环内容，接口文档已补四个导出接口与导出规则。
3. 当前 `test` 分支工作区干净，无未提交改动；上述闭环证据已在分支上提交。

### 10.3 admin 导出下载链路补证

| 验证对象 | 方式 | 结果 |
| --- | --- | --- |
| `bills` admin 导出 | 本地 `admin-web(5174)` 页面真实点击 `导出账单`，经 `127.0.0.1:8080 -> 18080` 代理命中真实 backend，校验返回 `attachment; filename=bills.csv` | 通过 |
| `cashier-sessions` admin 导出 | 本地 `admin-web(5174)` 页面真实点击 `导出会话`，经 `127.0.0.1:8080 -> 18080` 代理命中真实 backend，校验返回 `attachment; filename=cashier-sessions.csv` | 通过 |
| `refunds` admin 导出 | 本地 `admin-web(5174)` 页面真实点击 `导出退款单`，经 `127.0.0.1:8080 -> 18080` 代理命中真实 backend，校验返回 `attachment; filename=refunds.csv` | 通过 |
| `payment-flows` admin 导出 | 本地 `admin-web(5174)` 页面真实点击 `导出流水`，经 `127.0.0.1:8080 -> 18080` 代理命中真实 backend，校验返回 `attachment; filename=payment-flows.csv` | 通过 |

补充说明：

1. 该轮验证保留了真实页面路由、真实按钮事件与真实 URL 拼接逻辑，并经本地代理打到真实 payment-core backend。
2. 当前四块导出已不再缺“真实后端联调下载证据”；页面点击、真实导出 URL、真实 backend 响应头与 CSV 表头均已形成闭环。
3. 原始 `housekeeping_payment_core` 运行库曾缺少 `t_payment_task_lease`，但本轮已通过增量 DDL 补齐；同时还修复了任务中心升级告警递归膨胀导致的 `alert_content` 越界，原始运行库现已能成功完成 `PAYMENT_ISSUE_ESCALATE` 自动任务落库。

### 10.4 当前门禁判断

1. `payment-core` 四块导出在后端代码、模块测试、接口文档、自测记录层面已经闭环，不再属于 `test -> master` 的主要阻断项。
2. 当前仍不推进 `test -> master`，原因已经收敛到：
   - 原始 `payment-core` 运行库的关键结构迁移、任务中心运行时阻断和 `7` 条历史递归升级告警脏数据均已收口；这部分已不再构成 `test -> master` 阻断
   - `payment-core` 事件出站已新增 `13` 个定向用例，覆盖人工重投递语义、失败重试、重试耗尽进 `DEAD_LETTER`、无效事件拒绝与下游短路保护；但这仍属于应用层自动化门禁，不等同于真实 MQ 中间件生产级验证
   - 已新增 `docs/2026-07-31-预发布检查清单.md` 作为 release 前门禁跟踪骨架，但其内容仍明确标记为“预发布检查清单”而非正式 `release/*` 发布说明
   - 还缺 `test` 分支最终门禁审计结论，尚未形成可直接支撑 `master` 合并的整包复核口径
   - 既有跨系统阻断项仍包括 MQ 级可靠投递、失败重试、死信补偿、正式发布清单与发布后验证清单
3. 在上述证据未闭环前，不应基于 `master` 产出 `release/*` 文档和版本。

### 10.5 四块导出回归复核

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `bills / cashier-sessions / refunds / payment-flows` 服务层与控制器层定向回归 | `22` 个用例通过，`0` failures，`0` errors，`0` skipped | 已覆盖 CSV 条件归一化、字段输出与转义、下载响应和导出筛选条件下推；不重复替代第 10.3 节的真实页面下载验证 |

该复核再次确认四块导出能力不是当前合并阻断项。当前 `test -> master` 仍必须等待真实消息代理上的可靠投递、失败重试、死信队列与跨系统补偿演练，以及最终整包冻结复核完成。

### 10.6 clearing-system 重复消费幂等复核

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `ClearingEventServiceImplTest,ClearingEventDispatchServiceImplTest` 定向回归 | `4` 个用例通过，`0` failures，`0` errors，`0` skipped | 已覆盖支付成功事件正常消费、清分结果下游派发，以及同一 `paymentOrderId` 重复消费时不再重复生成清分单/分账项/本地事件 |

补充说明：

1. 本轮在 `clearing-system` 增加按 `paymentOrderId` 与 `PAYMENT_SUCCESS` 事件号口径的幂等判断，避免 `payment-core` 重投递再次命中清分消费端时把本地清分工件重复放大。
2. 这次修正关闭的是“应用内重复消费导致本地清分台账膨胀”的风险，不代表已经具备 MQ 级消费确认、失败重试、死信补偿或跨系统最终一致性证明。
3. 因此当前 `test -> master` 仍不成立；真实消息代理、跨系统补偿编排、正式发布清单与发布后验证清单依旧是剩余阻断项。

### 10.7 accounting / settlement 重复消费幂等复核

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `AccountingEventServiceImplTest` 定向回归 | `2` 个用例通过，`0` failures，`0` errors，`0` skipped | 已覆盖支付成功事件正常入账，以及同一 `paymentOrderId` 重复消费时余额与事件台账不再重复增加 |
| `SettlementEventServiceImplTest` 定向回归 | `2` 个用例通过，`0` failures，`0` errors，`0` skipped | 已覆盖清分事件正常生成结算单，以及同一 `clearingNo` 对同一结算对象重复消费时不再重复生成结算单与事件台账 |

补充说明：

1. `accounting-system` 本轮增加按 `accountNo + bizType + bizNo` 查询账务流水、按 `eventType + bizNo` 查询事件台账的幂等判断，关闭了重复支付成功或重复清分入账导致余额放大的风险。
2. `settlement-system` 本轮增加按 `clearingNo + targetType + targetNo` 查询既有结算单、按 `eventType + bizNo` 查询事件台账的幂等判断，关闭了重复清分事件导致结算单和明细项膨胀的风险。
3. 至此，本地 HTTP 链路下 `payment-core -> clearing-system -> accounting-system / settlement-system` 的重复消费放大量风险已被应用层自动化门禁覆盖，但这仍不等同于真实 MQ 的投递确认、消费确认、失败重试、死信转移和跨系统补偿编排证明。
4. 因此截至 `2026-07-31`，`test -> master` 仍不成立，也不基于 `master` 产出正式 `release/*` 文档或版本。

### 10.8 真实 MQ 接入与补偿演练方案落档

| 交付物 | 结果 | 说明 |
| --- | --- | --- |
| `docs/2026-07-31-真实MQ接入与跨系统补偿演练方案.md` | 已新增 | 已基于图稿、支付笔记、支付方案沉淀与陈天宇宙方法论，明确当前代码现状、推荐 RabbitMQ 接入方式、事件拓扑、幂等键、重试/DLQ/补偿策略和必做演练场景 |

补充说明：

1. 当前已不再是“只知道还缺 MQ 证据”，而是把“需要补哪些 broker 能力、哪些库表字段、哪些队列、哪些演练输出物”明确成正式方案。
2. 该方案仍属于设计与门禁拆解，不等同于真实 broker 已接入、真实 retry/DLQ 已运行或补偿演练已完成。
3. 因此当前 `test -> master` 依然不成立；但下一轮推进重点已经可以直接转入“按方案接 MQ 或按方案演练并收集证据”，而不是继续重复范围澄清。

### 10.9 RabbitMQ 测试环境与演练证据骨架

| 交付物 | 校验结果 | 结论 |
| --- | --- | --- |
| `infra/rabbitmq/docker-compose.yml` | `docker compose ... config` 通过 | RabbitMQ 3.13 management 测试环境可重复展开 |
| `infra/rabbitmq/definitions.json` | `jq empty` 通过；包含 `12` 个 durable queue、`6` 个 topic exchange、`12` 条 binding | 覆盖支付成功、清分生成两条链路的主队列、retry queue 与 DLQ |
| `docs/2026-07-31-MQ补偿演练记录模板.md` | 已新增 | 固化业务单号、消息 ID、队列快照、数据库事实和补偿审计证据口径 |

补充说明：

1. 本轮把 MQ 拓扑和演练证据格式从设计方案推进为可启动、可校验的仓库资产；默认重试 TTL 为 `5` 分钟，且每个消费者使用独立 retry queue，避免单一消费者失败时把原始广播事件重新投递给所有消费者。
2. 当前尚未启动 broker，也未改造四个应用的 AMQP publisher/consumer、发布确认、ACK/NACK 和任务中心补偿执行，因此这些资产不能替代真实运行证据。
3. 结论不变：截至本次审计，`test -> master` 不成立，不基于 `master` 创建正式 `release/*` 文档或版本。

### 10.10 payment-core AMQP 出站 relay 实现与回归

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `PaymentEventDispatchServiceImplTest` | `10` 个用例通过，`0` failures，`0` errors | 覆盖 `payment.trade / payment.success.v1` 发布、`eventNo` 的 `messageId/correlationId`、统一载荷、publisher confirm 与 broker 异常落库 |
| `payment-core` 全量 Maven 测试 | `203` 个用例通过，`0` failures，`0` errors，`0` skipped | Spring AMQP 依赖、既有 HTTP 派发和全部既有支付回归均通过 |

实现说明：

1. `payment-core` 已引入 `spring-boot-starter-amqp`，并新增 RabbitMQ 连接、mandatory publish、publisher confirm 和超时配置；凭据只从环境变量读取。
2. 当 `HSP_PAYMENT_EVENT_DISPATCH_MODE=amqp` 时，现有 `t_payment_event` outbox relay 发布一条统一的支付成功事件，由 `payment.trade` topic exchange 广播至 clearing/accounting 队列；broker 确认成功后才标记 outbox 成功，异常继续沿用 `FAILED -> DEAD_LETTER` 状态机和任务中心重发入口。
3. 默认模式保持 `http`，用于兼容尚未部署 RabbitMQ 的本地环境；切换到 `amqp` 必须显式配置 `HSP_RABBITMQ_PASSWORD` 并使用已提交的 `infra/rabbitmq` 拓扑。
4. 当前仍没有启动四系统真实 broker 链路，且 clearing/accounting/settlement 尚未完成 AMQP consumer、ACK/NACK、retry/DLQ 和补偿演练。因此本节不能作为 `test -> master` 放行依据，也不创建 `release/*` 文档或版本。

### 10.11 clearing-system 支付成功 AMQP consumer 实现与回归

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `ClearingPaymentSuccessAmqpConsumerTest` | `4` 个用例通过，`0` failures，`0` errors | 覆盖业务成功 ACK、失败转消费者独立 retry queue、达到上限转 DLQ、重投失败 NACK/requeue |
| `ClearingEventServiceImplTest` | `2` 个用例通过，`0` failures，`0` errors | 既有 `paymentOrderId` 幂等继续保证清分单、分账项和事件台账不重复膨胀 |
| `clearing-system` 全量 Maven 测试 | `10` 个用例通过，`0` failures，`0` errors，`0` skipped | AMQP 依赖、默认关闭 listener 的现有 HTTP 链路和清分回归均通过 |

实现说明：

1. `HSP_CLEARING_AMQP_ENABLED=true` 时，`clearing.payment-success` 监听 `payment.success.v1`，反序列化后仅调用既有 `ClearingEventService.consumePaymentSuccess`，不复制清分业务规则。
2. listener 使用手动 ACK；业务成功才 ACK。消费异常携带并递增 `x-retry-count` 转到 `payment.trade.retry`，超过 `3` 次转 `payment.trade.dlq`；发送 retry/DLQ 失败则 NACK 原消息并 requeue，避免静默丢失。
3. 当前 consumer 仍未连接真实 RabbitMQ 运行；后续章节已补齐 `clearing.generated.v1` 的 outbox relay 与 settlement/accounting consumers，但真实 broker 演练与补偿闭环证据仍缺失。因此 `test -> master` 仍不成立，不基于 `master` 创建正式 `release/*` 文档或版本。

### 10.12 accounting-system 支付成功 AMQP consumer 实现与回归

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `AccountingPaymentSuccessAmqpConsumerTest` | `3` 个用例通过，`0` failures，`0` errors | 覆盖账务成功 ACK、失败转消费者独立 retry queue、达到上限转 DLQ |
| `AccountingEventServiceImplTest` | `2` 个用例通过，`0` failures，`0` errors | 既有账务流水与事件台账幂等继续保证余额不重复增加 |
| `accounting-system` 全量 Maven 测试 | `7` 个用例通过，`0` failures，`0` errors，`0` skipped | AMQP 依赖、默认关闭 listener 的既有账务回归均通过 |

实现说明：

1. `HSP_ACCOUNTING_AMQP_ENABLED=true` 时，`accounting.payment-success` 监听 `payment.success.v1`，反序列化后仅调用既有 `AccountingEventService.consumePaymentSuccess`。
2. listener 使用手动 ACK 和消费者独立 retry/DLQ 路由；消费失败时保留 `messageId/correlationId`，用 `x-retry-count` 控制最大重试次数，重投失败时 NACK/requeue 原消息。
3. 后续章节已补齐 `clearing.generated.v1` 的 AMQP 出站与 settlement/accounting consumers；真实 broker 的端到端演练仍未完成。因此 `test -> master` 仍不成立，不基于 `master` 创建正式 `release/*` 文档或版本。

### 10.13 clearing-system 清分结果 outbox relay 实现与回归

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `ClearingEventDispatchServiceImplTest` | `3` 个用例通过，`0` failures，`0` errors | 覆盖 HTTP 回退派发、AMQP `clearing.trade / clearing.generated.v1` 投递、`eventNo` 消息标识和 publisher confirm |
| `ClearingEventServiceImplTest` | `2` 个用例通过，`0` failures，`0` errors | 覆盖清分生成与重复消费幂等；清分成功时同时创建单一 `CLEARING_GENERATED` 待投递事件 |
| `clearing-system` 全量 Maven 测试 | `11` 个用例通过，`0` failures，`0` errors，`0` skipped | outbox schema、mapper、AMQP relay、既有清分与消费者回归均通过 |

实现说明：

1. `t_clearing_event` 增加 `publish_status / retry_count / last_published_at / next_retry_at`，新库 schema 与 `systems/clearing-system/database/migrations/20260801_add_clearing_event_outbox.sql` 已同步。
2. 清分成功在本地事务内创建 `CLEARING_GENERATED` outbox；relay 在 AMQP 模式等待 publisher confirm 后标记 `SUCCESS`，异常标记 `FAILED` 并写入下次重试时间。默认 HTTP 模式保留用于未部署 broker 的环境。
3. `settlement-system` 与 `accounting-system` 已订阅 `clearing.generated.v1`；真实 broker 演练、retry/DLQ 队列观察和补偿闭环证据仍未完成。因此 `test -> master` 仍不成立，不基于 `master` 创建正式 `release/*` 文档或版本。

### 10.14 settlement-system 清分结果 AMQP consumer 实现与回归

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `SettlementClearingGeneratedAmqpConsumerTest` | `3` 个用例通过，`0` failures，`0` errors | 覆盖结算事实生成成功 ACK、失败转消费者独立 retry queue、达到上限转 DLQ |
| `SettlementEventServiceImplTest` | `2` 个用例通过，`0` failures，`0` errors | 既有 `clearingNo + targetType + targetNo` 幂等继续保证结算单和事件台账不重复膨胀 |
| `settlement-system` 全量 Maven 测试 | `9` 个用例通过，`0` failures，`0` errors，`0` skipped | AMQP 依赖、默认关闭 listener 的本地链路及既有结算/出款回归均通过 |

实现说明：

1. `HSP_SETTLEMENT_AMQP_ENABLED=true` 时，`settlement.clearing-generated` 监听 `clearing.generated.v1`，反序列化后仅调用既有 `SettlementEventService.consumeClearingGenerated`，保持结算事实创建与后续出款解耦。
2. listener 使用手动 ACK；消费失败时保留 `messageId/correlationId`，以 `x-retry-count` 控制重试上限，发送 retry/DLQ 失败则 NACK/requeue 原消息，异常格式的重试头按首次消费处理，避免消息因解析错误静默丢失。
3. `accounting-system` 已在后续章节订阅 `clearing.generated.v1`；clearing outbox 仍缺自动失败 relay，且无真实 broker 的端到端 retry/DLQ 与补偿演练证据。因此 `test -> master` 仍不成立，不基于 `master` 创建正式 `release/*` 文档或版本。

### 10.15 accounting-system 清分结果 AMQP consumer 实现与回归

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `AccountingClearingGeneratedAmqpConsumerTest` | `3` 个用例通过，`0` failures，`0` errors | 覆盖清分结果入账成功 ACK、失败转消费者独立 retry queue、达到上限转 DLQ |
| `AccountingEventServiceImplTest` | `2` 个用例通过，`0` failures，`0` errors | 既有 `accountNo + CLEARING_GENERATED_EVENT + clearingOrderNo` 幂等继续保证账务余额与事件台账不重复膨胀 |
| `accounting-system` 全量 Maven 测试 | `10` 个用例通过，`0` failures，`0` errors，`0` skipped | 两类 AMQP consumer、默认关闭 listener 的本地链路及既有账务回归均通过 |

实现说明：

1. `HSP_ACCOUNTING_AMQP_ENABLED=true` 时，`accounting.clearing-generated` 监听 `clearing.generated.v1`；清算 outbox 的统一载荷已包含 `accountNo / clearingOrderNo / bizNo / amount / summary`，consumer 直接调用既有 `AccountingEventService.consumeClearingGenerated`。
2. listener 使用手动 ACK，并通过专属 `clearing.trade.retry / clearing.trade.dlq` 与 `x-retry-count` 进行隔离重试；重投失败则 NACK/requeue 原消息，异常格式重试头按首次消费处理。
3. 清分 outbox 的 `FAILED` 仍缺自动调度重投，且无真实 broker 的端到端 retry/DLQ、重复消费与补偿演练证据。因此 `test -> master` 仍不成立，不基于 `master` 创建正式 `release/*` 文档或版本。

### 10.16 clearing-system 失败 outbox 自动重投实现与回归

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| `ClearingOutboxRetryServiceTest` | `2` 个用例通过，`0` failures，`0` errors | 覆盖到期失败事件仅在可解析支付单时重投，以及无到期事件时不执行 |
| `clearing-system` 全量 Maven 测试 | `13` 个用例通过，`0` failures，`0` errors，`0` skipped | 既有清分、AMQP publisher/consumer 与新增 outbox scheduler 回归均通过 |

实现说明：

1. 应用已启用调度；`clearing.outbox-retry.fixed-delay-ms` 默认 `300000`，`clearing.outbox-retry.batch-size` 默认 `100`。
2. mapper 仅检索 `CLEARING_GENERATED + FAILED + next_retry_at <= now` 的事件，服务层将 `bizNo` 解析为清分单后交给既有 publisher 重投，publisher 继续负责 publisher confirm 及成功/失败状态回写。
3. 自动调度重投已补齐，但真实 broker 的端到端 retry/DLQ、重复消费与补偿演练证据仍缺失。因此 `test -> master` 仍不成立，不基于 `master` 创建正式 `release/*` 文档或版本。

### 10.17 真实 MQ 成功链路与 payment-core 分页回归

| 验证范围 | 结果 | 说明 |
| --- | --- | --- |
| RabbitMQ 本地演练环境 | 通过 | RabbitMQ `3.13.7`、隔离 MySQL `8.0.46`、四个 Spring Boot 服务及四类 AMQP consumer 已同时在线；`12` 个业务/retry/DLQ 队列演练后均无积压 |
| 正式提交与回调 | 通过 | `PAY1785553233592` / `PRE1785553233596` 经 `housekeeping-h5-web` 规则提交和微信 H5 成功回调，支付详情显示终端、幂等键、路由规则和支付成功 outbox 事件 |
| 支付成功跨系统链路 | 通过 | `PAY1785552908678` 的 `EVT1785552908878` 由 payment outbox 投递；`CLO20003` 清分成功、账务 `EVT50006` 已消费、结算 `SLT20004` 待审核；清分 outbox 下游账务 `EVT50005` 与结算 `SVE70003` 均已消费 |
| publisher confirm 状态回写 | 通过 | payment-core 与 clearing-system 已在 `RabbitTemplate.invoke` 的同一 confirm scope 内发布并等待确认，避免消息已送达但 outbox 被误记为 `FAILED` |
| payment-core 全量 Maven 测试 | `203` 个用例通过 | `0` failures、`0` errors、`0` skipped；含 AMQP relay、配置规则、支付主流程与导出回归 |
| clearing-system 全量 Maven 测试 | `13` 个用例通过 | `0` failures、`0` errors、`0` skipped；含清分 outbox relay、AMQP consumer 与自动重投 |
| payment-core 列表真实接口 | 通过 | `payments`、`bills`、`cashier-sessions`、`refunds`、`orders`、`payment-flows`、`payment-logs`、`payment-requests`、`payment-routes`、`settlements/workers`、`payment-records` 均以 MyBatis + MySQL 成功返回分页数据；收款记录组合筛选亦通过 |

实现与问题收口：

1. 真实演练首次暴露 publisher confirm 使用范围错误：消息已投递，但确认等待发生在错误的 channel scope，导致 outbox 状态误记失败。已修复并以 payment-core `10/10`、clearing-system 定向 `3/3` 回归覆盖。
2. 演练还暴露控制策略种子商户号使用应用别名，无法匹配启用渠道。`data.sql` 与本地演练库已统一为实际渠道商户号，`housekeeping-h5-web` 自检为 `PASS`，正式提交链路通过。
3. 真实列表调用暴露全部 `@Param("query")` Mapper 的顶层分页参数绑定错误；已将相关 SQL 改为 `query.limit/query.offset`，并修正 `PaymentRecordMapper` 的筛选与排序参数路径。该批接口已在重启后的 payment-core 实例上验证。
4. 门禁结论仍为不通过：正常支付成功链路已有证据，但 broker 暂停恢复、自动重试、DLQ、非法消息和任务中心补偿闭环尚未全部实际执行与留档；未满足 `test -> master`，不得基于 `master` 创建正式 release 文档或版本。

### 10.18 broker 恢复、人工补偿与支付库重启保留演练

| 演练项 | 实际证据 | 结果 |
| --- | --- | --- |
| broker 离线出站失败 | 停止隔离 RabbitMQ 后，对 `PAY1785574551084` 成功回调，生成 `EVT1785574574727`；outbox 为 `FAILED`、`retry_count=1`、`next_retry_at=2026-08-01 09:01:14` | 通过 |
| broker 恢复人工补偿 | broker 管理面及四个消费者恢复后，任务中心手动重发处理 `1` 条并成功 `1` 条；outbox 变为 `SUCCESS`、`retry_count=3` | 通过 |
| 下游最终事实 | 清分单 `CLO20005` 为“清分成功”；账务事件 `EVT50009` 对应 `PAY1785574551084` 且状态为“已消费”；恢复后所有队列无积压 | 通过 |
| 外部 MySQL 重启保留 | 创建 `PAY1785574418627` / `PRE1785574418631` 后重启 payment-core；服务 API 与 MySQL 均能读取同一支付事实 | 通过 |

问题收口：

1. 原失败事件查询把“手动立即补偿”和“自动按退避窗口重试”混为一体，导致失败 outbox 尚未到 `next_retry_at` 时人工重发处理数为零。现拆分为 `findAllFailedEventNos` 与 `findDueFailedEventNos`：手动任务重发所有 `FAILED`，自动调度继续只消费到期事件。
2. 原 `spring.sql.init.mode=always` 会在连接外部 MySQL 时重放含清表语义的 `schema.sql/data.sql`。payment-core 默认已改为 `never`，仅显式 `demo` profile 或 `HSP_SQL_INIT_MODE=always` 可重建样例库。
3. payment-core 全量 Maven 回归 `203/203` 通过。虽然 broker 恢复和人工补偿已闭环，consumer retry/DLQ、非法消息和跨系统补偿处置仍待实际演练，因此 `test -> master` 仍不放行。

### 10.19 消费者 retry/DLQ 串扰修复

真实非法消息注入发现，原 `payment.success.retry.v1`、`payment.success.dlq.v1` 以及清分结果对应 routing key 被不同消费者共用；一侧失败会被 topic exchange 广播到另一侧的 retry/DLQ 队列。现已为 clearing、accounting、settlement 四类 consumer 设定独立 retry/DLQ key，并新增 `payment.trade.replay` 与 `clearing.trade.replay`，使 TTL 回流仅回到原 consumer 队列，不再重新广播。

| 验证范围 | 结果 |
| --- | --- |
| `infra/rabbitmq/definitions.json` | `jq empty` 通过 |
| clearing-system 全量测试 | `13/13` 通过 |
| accounting-system 全量测试 | `10/10` 通过 |
| settlement-system 全量测试 | `9/9` 通过 |

当前仍需按新 definitions 重载本地 broker，并重新执行非法消息和 TTL retry 演练；在真实隔离证据补齐前，`test -> master` 不放行。

### 10.20 消费者独立 retry/DLQ 实机回归

本轮重载 RabbitMQ definitions 后，发现存量下游进程仍加载旧编译产物：非法消息会被 ACK 并发送到已移除 binding 的共享 routing key。三套下游服务已使用当前源码和 JDK 8 重启，随后完成以下本地 broker 演练。

| 场景 | 注入与观察 | 结果 |
| --- | --- | --- |
| 最大次数非法支付事件 | 向 `payment.trade/payment.success.v1` 注入 `x-retry-count=3` 的非法 JSON；clearing/accounting 各有 `1` 条进入专属 DLQ | 通过，无串扰 |
| 最大次数非法清分事件 | 向 `clearing.trade/clearing.generated.v1` 注入 `x-retry-count=3` 的非法 JSON；settlement/accounting 各有 `1` 条进入专属 DLQ | 通过，无串扰 |
| TTL 回流隔离 | 对两类事件注入 `x-retry-count=2` 的非法 JSON；四条 retry queue 各暂存 `1` 条，`300` 秒后均清零，各自 DLQ 由 `1` 条增至 `2` 条 | 通过，回流不再广播到其他 consumer |

实机快照确认四个主队列均有 `1` 个 consumer，所有主队列与 retry queue 最终均为 `0` 条；`clearing.payment-success.dlq`、`accounting.payment-success.dlq`、`settlement.clearing-generated.dlq`、`accounting.clearing-generated.dlq` 各为 `2` 条演练死信。四个 consumer 单测亦改为断言专属 routing key，防止测试再次掩盖生产配置漂移。

本项关闭了 consumer retry/DLQ 隔离与 TTL 回流的实机风险，但 `DLQ -> 补偿工单/任务中心 -> 人工处置/重投 -> 审计` 尚未形成跨系统可执行闭环；因此 `test -> master` 仍不放行，且不基于 `master` 创建正式 `release` 文档或版本。

### 10.21 DLQ 补偿任务 intake 与人工处置实机验证

为保留 broker 死信证据，同时让 payment-core 形成可审计的处置入口，新增 `payment.compensation.dlq-intake` durable queue。它与四个原 DLQ 共同订阅各自的 DLX routing key，payment-core listener 仅在任务账本成功持久化后 ACK intake 副本；原始 DLQ 不被消费或删除。任务表以 `message_id + dead_letter_routing_key` 幂等，记录消息体/头快照、原目标系统、定向 replay exchange/key、处置人、说明、状态与重放次数。

| 演练项 | 结果 |
| --- | --- |
| `DRILL-DLQ-TASK-20260801-001` 最大重试非法支付消息 | clearing/accounting 原 DLQ 各保留 `1` 条；payment-core 自动生成 `2` 条 `PENDING_REVIEW` 任务，分别绑定 `payment.success.clearing.replay.v1` 与 `payment.success.accounting.replay.v1` |
| 人工不可重放处置 | 两条任务均由“支付运营”记录为 `MANUAL_RESOLVED`，保留“消息体非合法 JSON，不可安全重放”的处置说明与时间 |
| 定向重放实现 | 仅允许人工先转为 `READY_TO_REPLAY`，再使用 publisher confirm 向专属 `*.replay.v1` routing key 发送，并清除 `x-retry-count`；失败回写 `REPLAY_FAILED`，避免把旧重试次数直接送回 DLQ |

新增 payment-core 迁移 `systems/payment-core/database/migrations/2026-08-01-add-payment-dead-letter-task.sql` 已在隔离 MySQL 执行。新增单测覆盖 intake 幂等路由、人工复核前置、定向重放、publisher confirm 失败留痕和 listener ACK/NACK；payment-core 全量 Maven 回归 `213/213` 通过。

门禁仍不通过：当前已证明“DLQ -> 可审计任务 -> 人工结案”闭环；仍需用可修复的真实业务失败事件完成一次“人工修复 -> 定向 replay -> 下游成功事实 -> 任务结果复核”演练，并补齐最终冻结复核，才可评估 `test -> master`。

### 10.22 人工复核后定向 replay 与下游事实验证

为验证已耗尽重试但消息业务字段完整的恢复路径，向 accounting 专属 DLX 注入 `DRILL-DLQ-REPLAY-20260801-001`，消息体为支付成功事件，目标账户为已存在的 `ACT10003`。payment-core 自动生成单条 `PENDING_REVIEW` 任务 `DLQ0341f6e1dd604dff8b1b`，其路由固定为 `payment.trade.replay / payment.success.accounting.replay.v1`。

| 步骤 | 审计事实 | 结果 |
| --- | --- | --- |
| 人工复核 | 支付运营确认账户存在且消息字段完整，任务转为 `READY_TO_REPLAY` | 通过 |
| 定向 replay | publisher confirm 成功后任务转为 `REPLAYED`，`replay_count=1`，重放未带 `x-retry-count` | 通过 |
| 下游账务事实 | `ACT10003` 可用余额由 `¥12.00` 增至 `¥13.23`；产生唯一 `PAYMENT_SUCCESS` 消费事件 `EVT50003` | 通过 |
| 非目标系统隔离 | clearing 以该支付单号查询结果为 `0`，未生成新的清分事实 | 通过 |
| 重复投递幂等 | 再向同一 accounting replay key 投递相同 `messageId` 后，余额仍为 `¥13.23`，消费事件仍为 `1` 条 | 通过 |

该场景是“消息已耗尽重试、下游恢复后人工重放”的真实 broker 运行演练，并非伪造下游成功；它关闭了补偿任务的定向重放、下游事实与幂等保护证据缺口。当前仍需执行整包冻结复核、核对所有必做场景的版本和证据完整性，复核通过前仍不得执行 `test -> master` 或创建正式 release 文档与版本。

### 10.23 2026-08-01 test 分支冻结复核

本次复核基于 `test` 当前头部 `40bd54e`，执行时间为 `2026-08-01 22:03 +08:00`。所有后端 Maven 测试均使用 JDK 8 与仓库锁定的 Maven 本地仓库执行。

| 系统 | 测试结果 |
| --- | --- |
| `payment-core` | `213/213` 通过，`0` failures，`0` errors |
| `wallet-system` | `5/5` 通过，`0` failures，`0` errors |
| `gateway-access` | `8/8` 通过，`0` failures，`0` errors |
| `clearing-system` | `13/13` 通过，`0` failures，`0` errors |
| `settlement-system` | `9/9` 通过，`0` failures，`0` errors |
| `accounting-system` | `10/10` 通过，`0` failures，`0` errors |

实时 broker 复核显示五个业务与补偿 consumer 均在线，主队列和 retry 队列均为 `0`。原始 DLQ 留存三条演练证据：`clearing.payment-success.dlq=1`、`accounting.payment-success.dlq=2`；payment-core 的 `t_payment_dead_letter_task` 与 API 均显示三条任务已结案，其中两条 `MANUAL_RESOLVED`、一条 `REPLAYED` 且 `replay_count=1`。这些消息为保留的演练审计副本，不是未处置的生产积压。

本次冻结结论：**不允许 `test -> master`，不创建 `release/*` 或版本。** 阻断项不是后端回归或 MQ 补偿闭环，而是：

1. `40bd54e` 于 `2026-08-01 21:59 +08:00` 才进入 `test`，尚未完成仓库治理要求的一轮稳定观察。
2. 九个前端生产构建仅有此前记录，未在本次冻结基线重新执行，不能替代当前版本的前端验证。
3. 因此当前尚不能确认“最新 test 分支无阻断级已知问题”的完整门禁条件；稳定观察和前端构建完成后，须重新执行本节复核再决定是否合并。

### 10.24 当前基线九端前端生产构建

于 `2026-08-01 22:10 +08:00`，在 `test` 头部 `0dd7014` 对以下前端工程执行 `npm run build`，九个目标均以 Vite 生产构建成功退出。

| 系统/端 | 结果 |
| --- | --- |
| `payment-core/admin-web` | 通过 |
| `payment-core/app-web` | 通过 |
| `payment-core/h5-web` | 通过 |
| `payment-core/pc-web` | 通过 |
| `wallet-system/admin-web` | 通过 |
| `gateway-access/admin-web` | 通过 |
| `clearing-system/admin-web` | 通过 |
| `settlement-system/admin-web` | 通过 |
| `accounting-system/admin-web` | 通过 |

至此当前基线的六个后端全量测试和九端前端生产构建均已完成，工作区亦保持干净。`master...test` 为 `0 276`，即 `test` 包含 `master` 全部提交并额外领先 276 个提交；本轮范围和风险需在合并说明中明确记录。

门禁结论仍为不通过：最后一项业务提交 `40bd54e` 的时间为 `2026-08-01 21:59 +08:00`，本次构建完成时仅稳定观察约 11 分钟。稳定观察完成前，不能声明 `test` 无新增阻断问题，故不执行 `test -> master`，也不创建正式 release 文档或版本。

### 10.25 test -> master 最终合并判定

于 `2026-08-02 05:42 +08:00` 完成最终复核。自最后业务提交 `40bd54e`（`2026-08-01 21:59 +08:00`）以来，`test` 未出现新的业务代码提交；稳定观察已超过五小时。当前 `test` 工作区干净，`git diff --check` 通过，且 `master...test` 为 `0 277`，可进行快进合并。

合并范围：payment-core 的支付、退款、配置、任务中心、四块经营导出与 DLQ 补偿闭环；以及 wallet、gateway-access、clearing、settlement、accounting 的资金基础能力与 RabbitMQ 事件链路。本次范围以当前 `test` 的 277 个增量提交为准。

最终门禁：**允许 `test -> master`。** 依据为审计 10.17 至 10.24 的真实链路、DLQ 人工处置、六个后端全量测试和九端前端生产构建证据，以及本次稳定观察。风险边界为：三条原始 DLQ 演练副本仍保留在 broker 用于审计，但对应的三条 payment-core 任务均已结案，不属于未处置积压。回滚边界为：若 master 稳定观察发现阻断问题，代码回退至合并前 master `18fdf2d`；数据库采用已提交的增量迁移回滚方案，不删除既有支付、清分、结算和账务事实。

`master` 合并后仍**不立即创建 `release/*` 或版本**：须按《发布门槛与版本治理规范》在 master 完成一轮稳定观察，再基于该稳定基线填写正式发布说明、版本范围、数据库/接口/前端变更、风险、回滚和发布后验证项。

### 10.26 master 稳定观察状态

`master` 已于 `2026-08-02 05:42 +08:00` 快进至获批基线 `c8e10ce`。`2026-08-02 06:00 +08:00` 的运行态复核显示五个业务/补偿 consumer 在线，主队列和 retry 队列均无积压，三条 DLQ 演练副本仍分别对应已结案任务；当前没有新增运行时阻断。

但 master 稳定观察仅约 17 分钟，尚未满足“master 已稳定观察至少一轮”的发布门槛。因此**不创建正式 release 文档，不创建版本标签**；后续仅在观察期内无新阻断项时重新评估。

### 10.27 远端同步完成后的 release 复核

于 `2026-08-01` 已完成 `git push origin test master`，远端头指针回读为：

- `origin/test = c8e10ce`
- `origin/master = 8e0998f`

当前本地 `master...origin/master` 为 `0 0`，说明 `test -> master` 的远端同步已完成；`master...test` 为 `1 0`，说明 `master` 仅比 `test` 多一条发布审计文档提交，业务基线仍为 `c8e10ce`。

同轮运行态复核结果如下：

1. RabbitMQ 仍有 `5` 个业务/补偿 consumer 在线。
2. `payment.compensation.dlq-intake`、四个主消费队列与四个 retry 队列均无积压。
3. broker `queue_totals.messages_ready = 3`，拆到队列级后确认是三条保留中的原始 DLQ 演练副本：`clearing.payment-success.dlq = 1`、`accounting.payment-success.dlq = 2`；这与 payment-core 任务中心 API 中两条 `MANUAL_RESOLVED` 和一条 `REPLAYED` 的结案记录一致，不属于新增未处置积压。

但本轮还发现一个新的 release 阻断：**时间源不可信**。当前实际日期是 `2026-08-01`，而以下运行态与提交元数据均返回了未来时间 `2026-08-02`：

1. `c8e10ce` 的 commit time 为 `2026-08-02T05:42:56+08:00`
2. `8e0998f` 的 commit time 为 `2026-08-02T06:16:59+08:00`
3. payment-core dead-letter task API 的 `requestId` 生成时间前缀为 `REQ20260802...`

这意味着当前无法用仓库提交时间、服务端时间或本机 shell 时间来证明“master 已完成一轮稳定观察”。因此本节结论为：**远端交付完成，但 `master -> release` 仍不放行**。必须先校正相关主机/容器时间，再基于可信时间源重新开始 master 稳定观察，之后才允许创建正式 release 文档和版本。

### 10.28 时间源阻断的修复前置条件

于 `2026-08-01` 进一步核对，时间漂移并非单点现象，而是整条运行链路共用同一未来时间：

1. 宿主机 `date` 返回 `2026-08-02 07:54:42 CST +0800`
2. RabbitMQ 管理接口 `Date` 响应头为 `Sat, 01 Aug 2026 23:55:50 GMT`
3. payment-core API `Date` 响应头为 `Sat, 01 Aug 2026 23:56:13 GMT`
4. payment-core API `requestId` 前缀为 `REQ20260802...`

其中 2 和 3 折算到 `Asia/Shanghai` 后均为 `2026-08-02 07:55~07:56 +08:00`，与宿主机时间一致，说明 RabbitMQ 容器和 Spring Boot 服务都在继承宿主机的未来时间，而不是各自独立漂移。

当前还能确认两点边界：

1. RabbitMQ 运行态仍稳定：`5` 个业务/补偿 consumer 在线，主消费队列和 retry 队列无积压；`queue_totals.messages_ready = 3` 仍是三条审计保留的原始 DLQ 副本。
2. `systemsetup -getusingnetworktime` 与 `systemsetup -getnetworktimeserver` 均要求管理员权限；本轮无法在当前权限下直接查看或修复 macOS 网络授时设置。

因此后续 release 放行前的前置动作已经明确，不再属于代码交付缺口，而是环境修复清单：

1. 由具备管理员权限的操作者校正 macOS 宿主机日期与时间，确保当前为 `2026-08-01 Asia/Shanghai` 的真实时间。
2. 校正后重启 payment-core、RabbitMQ 及相关演练服务，复核宿主机 `date`、RabbitMQ/ payment-core HTTP `Date` 头、payment-core `requestId` 前缀不再落入 `2026-08-02`。
3. 仅在时间源恢复可信后，重新开始 `master` 的稳定观察窗口；观察期结束且无新增阻断项，才允许创建正式 release 文档和版本。

### 10.29 时间源恢复与 master 观察重启

于 `2026-08-02 08:11 +08:00` 重新核对，宿主机 `date` 已返回 `2026-08-02 08:11:05 CST +0800`，与当前运行日期一致。RabbitMQ 管理接口和 payment-core 接口可正常响应，payment-core 的 `requestId` 亦为 `REQ20260802...`；因此此前将 `2026-08-02` 视为未来日期的判断已不再成立，时间源阻断关闭。

同轮复核结果如下：

1. `master = origin/master = 10e559d`，工作区干净；`origin/test = c8e10ce`，`master...test = 3 0`，差异仅为三条发布审计文档提交，获批业务基线仍为 `c8e10ce`。
2. RabbitMQ 有 `5` 个业务/补偿 consumer 在线，四个主消费队列、四个 retry 队列与 `payment.compensation.dlq-intake` 均无积压。
3. `queue_totals.messages_ready = 3`，仅来自 `clearing.payment-success.dlq = 1`、`accounting.payment-success.dlq = 2` 的审计留存；payment-core API 显示对应任务均已结案（两条 `MANUAL_RESOLVED`、一条 `REPLAYED`），没有待处置 dead-letter task。
4. 使用本机已安装的 Oracle JDK `1.8.0_202` 于 `2026-08-02 08:17 +08:00` 重跑 `systems/payment-core/backend` 全量 Maven 测试，Surefire 汇总为 `213` tests、`0` failures、`0` errors、`0` skipped。默认 `java` 指向 JRE 时会报“no compiler is provided”，因此本次显式设置了 JDK `JAVA_HOME`；这属于本机命令环境修正，不是项目编译缺陷。
5. 随后使用相同 JDK 复核消息下游：`clearing-system` 为 `13` tests、`settlement-system` 为 `9` tests、`accounting-system` 为 `10` tests，均为 `0` failures、`0` errors、`0` skipped。该组回归覆盖 payment success、clearing generated 两段 AMQP 消费、幂等与 outbox 相关实现。
6. `wallet-system` 与 `gateway-access` 同样使用该 JDK 完成全量 Maven 回归，分别为 `5` tests 和 `8` tests，均为 `0` failures、`0` errors、`0` skipped。连同前述四个后端，本轮六个支付核心系统合计 `258` tests 通过。
7. 于 `2026-08-02 08:30~08:34 +08:00` 重跑九端 `npm run build`：payment-core 的 admin/app/H5/PC 四端，以及 wallet、gateway-access、clearing、settlement、accounting 的 admin 端全部以 Vite 生产构建成功退出。构建后 `git diff --check` 通过且工作区干净。

当前结论：**时间源已不再阻断 release，但 master 稳定观察刚重新开始，尚不创建正式 release 文档或版本标签。** 后续需在观察窗口结束后重新复核远端、运行态及未结案补偿任务；无新增阻断项时，才可按发布模板创建版本说明并切 `release/*`。

### 10.30 v1.0.0 release 最终放行

于 `2026-08-02 08:38 +08:00` 完成最终门禁审计。可信时间源上的 master 已形成多次运行态快照，期间业务基线 `c8e10ce` 未发生代码、数据库或前端漂移；后续提交仅补充门禁证据并已同步至 test。

最终证据如下：

1. 六个支付后端当前基线 Maven 回归合计 `258` tests，均为 `0` failures、`0` errors、`0` skipped；九端 Vite 生产构建均通过。
2. RabbitMQ 有 `5` 个业务/补偿 consumer 在线，主消费、retry 与 intake 队列无积压；3 条保留 DLQ 均有结案补偿任务对应，无新增待处置任务。
3. `master`、`test` 及其远端分支已同步，工作区干净，`master...test = 0 0`。
4. 正式发布说明已按模板补齐版本范围、数据库、接口、前端、测试、风险、回滚和发布后验证。

最终门禁：**允许创建 `release/v1.0.0` 与 `v1.0.0` 标签。** 发布后仍必须按版本说明执行环境密钥、数据库备份与核心支付/退款/任务中心/MQ 运行态验证；本地演练不能替代生产发布后观察。

### 10.31 2026-08-03 clearing.generated 快速补偿回流补证

本轮不是新的发布放行动作，而是在当前 `test` 工作线上继续补齐 `clearing.generated.*` 的隔离 MQ 证据。运行环境如下：

1. `payment-core`：`18081`，连接本机 MySQL `127.0.0.1:3306 / housekeeping_payment_core`
2. `clearing-system`：`18122`
3. `settlement-system`：`18132`
4. `accounting-system`：`18112`
5. RabbitMQ：`hsp-rabbitmq`
6. 隔离后缀：`20260803fast`
7. `RETRY_TTL_MS=2000`

本轮新增事实：

| 项目 | 方式 | 结果 |
| --- | --- | --- |
| 快速隔离拓扑恢复 | `RETRY_TTL_MS=2000 bash infra/rabbitmq/declare_isolated_topology.sh 20260803fast` | 成功恢复 `payment.success / clearing.generated` 的 fast drill 队列集 |
| 四个后端服务恢复 | 本地 JDK 8 + Maven 前台启动 | `payment-core / clearing / settlement / accounting` 均可连上 RabbitMQ，本机端口分别为 `18081 / 18122 / 18132 / 18112` |
| 第一轮非法清分结果消息 | 向 `clearing.trade / clearing.generated.20260803fast.v1` 注入 `MSG-FAST-CLG-20260803-001` | `settlement/accounting` 各生成 1 条 `PENDING_REVIEW` 补偿任务 |
| 第二轮非法清分结果消息 | 向 `clearing.trade / clearing.generated.20260803fast.v1` 注入 `MSG-FAST-CLG-20260803-002` | `settlement.clearing-generated.20260803fast.dlq=2`、`accounting.clearing-generated.20260803fast.dlq=2` |
| 任务账本核对 | `GET /api/payment-dead-letter-tasks?messageId=MSG-FAST-CLG-20260803-001/002` | 共新增 4 条任务，目标系统分别为 `settlement-system`、`accounting-system` |

关键审计证据：

1. 四条新任务的 `headerSnapshot` 全部包含 `x-retry-count=3`。
2. 四条新任务均包含 `x-death.reason=expired`，证明消息经过 retry TTL 回流后再次失败。
3. `x-death.queue` 精确指向：
   - `settlement.clearing-generated.20260803fast.retry`
   - `accounting.clearing-generated.20260803fast.retry`
4. 对应死信路由为：
   - `clearing.generated.settlement.dlq.20260803fast.v1`
   - `clearing.generated.accounting.dlq.20260803fast.v1`

门禁判断更新：

1. 到这里，`payment.success.*` 与 `clearing.generated.*` 两条主补偿链路都已具备“隔离 MQ + retry TTL 回流 + 专属 DLQ + payment-core 补偿任务入账”的真实证据。
2. 当前剩余阻断项已经不再是 `clearing.generated.*` 缺实证，而是：
   - 本轮新增 4 条补偿任务尚未继续做人工结案或定向 replay 处置演练
   - `test` 当前工作线与历史 `master/release` 结论之间仍需做一版新的统一冻结审计
3. 因此本轮结论是：**补偿链路证据继续收敛，但当前不直接触发新的 `test -> master` 或 `release/*` 判定。**

### 10.32 2026-08-03 clearing.generated 补偿任务人工收口

在 10.31 的 4 条新增任务生成后，又继续完成了 payment-core 任务台账的人工结案收口：

| messageId | 任务数 | 最终状态 | 处理口径 |
| --- | --- | --- | --- |
| `MSG-FAST-CLG-20260803-001` | `2` | 全部 `MANUAL_RESOLVED` | 非法 JSON 报文，清分结果结构不完整，不具备安全重放条件 |
| `MSG-FAST-CLG-20260803-002` | `2` | 全部 `MANUAL_RESOLVED` | 非法 JSON 报文，清分结果结构不完整，不具备安全重放条件 |

审计事实：

1. 四条任务的执行人均为 `支付运营A`。
2. 结案时间落在 `2026-08-03 14:21:03 ~ 14:21:08 +0800`。
3. 结案后再次调用 `GET /api/payment-dead-letter-tasks?messageId=MSG-FAST-CLG-20260803-001/002`，返回结果均为 `MANUAL_RESOLVED`。
4. `2026-08-03 14:21:54 +0800` 的队列快照显示：
   - `settlement.clearing-generated.20260803fast.dlq=2`
   - `accounting.clearing-generated.20260803fast.dlq=2`
   - `payment.compensation.dlq-intake=0`

门禁判断进一步更新：

1. `clearing.generated.*` 本轮已经不只是“重试回流 + 入账”，而是完整到了“专属 DLQ -> payment-core 补偿任务 -> 人工处置收口”。
2. 因此当前 `test` 分支继续阻断的重点，已经上升为：
   - 需要基于截至 `2026-08-03` 的最新事实重做一版统一冻结审计
   - 需要继续推进支付平台其余子系统和跨端链路，而不是继续重复补这条已闭环的异常链路

### 10.33 当前 test 头整包回归复核

本轮基于当前 `test` HEAD `9f4f8cd`（`test` 相对 `master` 为 `0/55`，即 `test` 领先 `master` 55 个提交）重新核对整包回归证据。未跟踪的 `.runtime/` 仅包含本地服务运行日志，不纳入交付提交。

#### 后端全量 Maven 回归

使用项目约定的 JDK 8 与 Maven 本地仓库执行六个后端模块全量测试，Surefire 报告汇总如下：

| 系统 | 测试套件 | 测试数 | 失败 | 异常 | 跳过 | 结果 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `payment-core` | 56 | 261 | 0 | 0 | 0 | 通过 |
| `wallet-system` | 5 | 17 | 0 | 0 | 0 | 通过 |
| `gateway-access` | 2 | 20 | 0 | 0 | 0 | 通过 |
| `clearing-system` | 12 | 29 | 0 | 0 | 0 | 通过 |
| `settlement-system` | 8 | 28 | 0 | 0 | 0 | 通过 |
| `accounting-system` | 12 | 32 | 0 | 0 | 0 | 通过 |
| **合计** | **95** | **387** | **0** | **0** | **0** | **通过** |

#### 前端生产构建

以下九个前端入口均执行 `npm run build` 成功：

1. `payment-core/frontend/admin-web`
2. `payment-core/frontend/app-web`
3. `payment-core/frontend/h5-web`
4. `payment-core/frontend/pc-web`
5. `wallet-system/frontend/admin-web`
6. `gateway-access/frontend/admin-web`
7. `clearing-system/frontend/admin-web`
8. `settlement-system/frontend/admin-web`
9. `accounting-system/frontend/admin-web`

#### 当前门禁结论

1. 当前 `test` 头的后端自动化回归和九端前端生产构建均通过，说明本轮控制器测试补强没有引入可见的编译或自动化回归问题。
2. 当前仍不执行 `test -> master`，原因不是本轮测试失败，而是当前 `HEAD` 的最新提交发生在 `2026-08-04`，稳定观察窗口刚重新开始；现阶段不能据此证明 `test` 已经完成一轮稳定观察。
3. 当前 `master` 仍停留在历史发布基线，`test` 领先 55 个提交；因此历史 `v1.0.0` release 结论不能覆盖当前 `test` 头。
4. 本轮没有新增真实第三方支付渠道、银行出款、密钥托管、生产数据库备份恢复或生产 RabbitMQ 演练证据；这些仍属于发布前必须明确的环境级边界，不能用单元测试替代。
5. 后续动作：基于当前 `test` HEAD 开始稳定观察；观察窗口、运行态、数据库迁移、跨系统消息和补偿任务均无新增阻断后，才重新评估 `test -> master`，随后才允许创建新的 release 文档和版本标签。

### 10.34 2026-08-04 refund-center V1 纳入整包回归

本轮基于 `test` HEAD `bdaca69` 复核。该提交新增独立 `refund-center`，并已同步到 `origin/test`；`test` 相对 `master` 为 `0/58`，即领先 58 个提交。

#### 后端全量回归

七个后端模块的 Surefire 报告汇总如下：

| 系统 | 测试套件 | 测试数 | 失败 | 异常 | 跳过 | 结果 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `payment-core` | 56 | 261 | 0 | 0 | 0 | 通过 |
| `wallet-system` | 5 | 17 | 0 | 0 | 0 | 通过 |
| `gateway-access` | 2 | 20 | 0 | 0 | 0 | 通过 |
| `clearing-system` | 12 | 29 | 0 | 0 | 0 | 通过 |
| `settlement-system` | 8 | 28 | 0 | 0 | 0 | 通过 |
| `accounting-system` | 12 | 32 | 0 | 0 | 0 | 通过 |
| `refund-center` | 2 | 8 | 0 | 0 | 0 | 通过 |
| **合计** | **97** | **395** | **0** | **0** | **0** | **通过** |

#### 前端生产构建

以下十个前端入口均执行 `npm run build` 成功：

1. `payment-core/frontend/admin-web`
2. `payment-core/frontend/app-web`
3. `payment-core/frontend/h5-web`
4. `payment-core/frontend/pc-web`
5. `wallet-system/frontend/admin-web`
6. `gateway-access/frontend/admin-web`
7. `clearing-system/frontend/admin-web`
8. `settlement-system/frontend/admin-web`
9. `accounting-system/frontend/admin-web`
10. `refund-center/frontend/admin-web`

#### 当前有效门禁结论

1. 本轮退款中心的后端单元测试、H2 DDL/MyBatis 集成测试和前端生产构建均通过，新增系统已纳入整包回归。
2. 当前仍不执行 `test -> master`，不是因为测试失败，而是 `bdaca69` 刚于 `2026-08-04 08:28:18 +0800` 进入 `test`，稳定观察窗口尚未完成。
3. 退款中心的真实第三方渠道请求、回调验签、RabbitMQ outbox relay、MySQL 实库迁移和渠道对账仍未取得运行态证据，不能用本地 H2 或模拟回调替代。
4. 稳定观察期内若无新增阻断，并完成上述生产化边界验证，才重新评估 `test -> master`；在此之前不创建新的 release 文档或版本标签。

### 10.35 2026-08-04 reconciliation-system V1 纳入整包回归

本轮基于当前 `test` HEAD `38ff6fc` 复核。`test` 已同步 `origin/test`，相对 `master` 为 `0/60`，即领先 60 个提交。

#### 后端全量回归

八个后端模块的 Surefire 报告汇总如下：

| 系统 | 测试数 | 失败 | 异常 | 跳过 | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| `payment-core` | 261 | 0 | 0 | 0 | 通过 |
| `wallet-system` | 17 | 0 | 0 | 0 | 通过 |
| `gateway-access` | 20 | 0 | 0 | 0 | 通过 |
| `clearing-system` | 29 | 0 | 0 | 0 | 通过 |
| `settlement-system` | 28 | 0 | 0 | 0 | 通过 |
| `accounting-system` | 32 | 0 | 0 | 0 | 通过 |
| `refund-center` | 8 | 0 | 0 | 0 | 通过 |
| `reconciliation-system` | 1 | 0 | 0 | 0 | 通过 |
| **合计** | **396** | **0** | **0** | **0** | **通过** |

#### 前端生产构建

以下十一个前端入口均执行 `npm run build` 成功：

1. `payment-core/frontend/admin-web`
2. `payment-core/frontend/app-web`
3. `payment-core/frontend/h5-web`
4. `payment-core/frontend/pc-web`
5. `wallet-system/frontend/admin-web`
6. `gateway-access/frontend/admin-web`
7. `clearing-system/frontend/admin-web`
8. `settlement-system/frontend/admin-web`
9. `accounting-system/frontend/admin-web`
10. `refund-center/frontend/admin-web`
11. `reconciliation-system/frontend/admin-web`

#### 当前有效门禁结论

1. 对账中心已纳入整包后端和前端验证，自动匹配、差异识别和后台构建均通过。
2. 当前仍不执行 `test -> master`：`38ff6fc` 是本轮新系统业务提交，稳定观察窗口尚未完成。
3. 对账中心的真实渠道账单下载/解析、异步大批次任务、银行流水认领和生产 MQ 事实订阅尚无运行态证据；不能用本地演示数据替代。
4. 继续在 `test` 上推进保证金系统和对账生产化能力；只有在整包稳定观察、数据库迁移、消息补偿和外部渠道边界均具备证据后，才评估 `master` 和新的 release。

### 10.36 2026-08-04 deposit-system V1 纳入交付检查

本轮新增独立 `deposit-system`，覆盖保证金账户生命周期和账户流水。

#### 已完成

1. 后端完成开户、收取、冻结、解冻、扣罚、退还和欠款抵扣。
2. MyBatis SQL 增加余额/冻结额期望值条件更新，流水增加冻结额前后快照。
3. 管理端覆盖账户总览、开户、账户作业和资金流水。
4. 独立 MySQL 数据库脚本与接口、后端、数据库、自测文档已补齐。
5. 单系统后端自动化测试：`3 tests, 0 failures, 0 errors, 0 skipped`。
6. 当前 12 个前端入口均完成 `npm run build`，保证金管理端构建产物生成成功。

#### 尚未形成门禁证据

1. 保证金真实收取和退款渠道尚未接入。
2. 保证金到账、扣罚、退还与账务系统的 RabbitMQ 事件尚未完成生产拓扑演练。
3. MySQL 实库迁移、备份恢复、并发压测和幂等表验证尚未完成。
4. 当前只证明 H2 联调基线通过，不能替代生产运行态证据。

#### 当前门禁结论

保证金系统 V1 可以继续留在 `test` 进行整包回归，但不触发 `test -> master`，也不创建新的 release。待前端构建和整包回归完成后，重新计算当前 HEAD 的全量证据。

#### 当前 HEAD 回归快照

本轮保证金变更后，当前 `test` 工作区实际已验证：

- 后端 10 个模块合计 `418 tests, 0 failures, 0 errors, 0 skipped`
- 前端 12 个入口全部 `npm run build` 通过，包含 `wallet-account` 和 `deposit-system`
- 本轮没有新增真实 MySQL、RabbitMQ、第三方收款/退款渠道和生产压测证据

该快照只说明自动化回归和前端构建通过，不改变当前“不合并 master、不发布 release”的门禁结论。

### 10.45 2026-08-06 risk-control-system 人工复核状态门禁补强

本轮围绕 `risk-control-system` 的人工复核状态流补强，目标是避免风控审核单被重复通过、重复拒绝或在并发审核下被后写请求覆盖。

#### 已完成

1. `RiskControlServiceImpl.reviewAction` 新增复核单存在性校验，并明确仅 `PENDING` 状态允许执行 `APPROVE / REJECT`。
2. `RiskControlMapper.xml` 的复核更新语句新增 `status = 'PENDING'` 数据库门禁，确保并发场景下只有首个审核动作落库成功。
3. 新增自动化回归用例，覆盖“先生成待审复核单，再审核通过，随后重复审核被拒绝”的业务链路。
4. 风控系统单系统文档已同步更新：后端详细设计、接口文档、自测记录、总体交付状态说明。

#### 本轮验证

1. 执行时间：`2026-08-06 09:49 +08:00`
2. 执行命令：

```bash
JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home \
PATH=$JAVA_HOME/bin:$PATH \
/Users/abc123/apache-maven-3.9.16/bin/mvn \
-Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository test -DskipITs=false
```

3. 执行目录：`systems/risk-control-system/backend`
4. 结果：`6 tests, 0 failures, 0 errors, 0 skipped`

#### 尚未形成门禁证据

1. 当前仅补强单系统审核状态门禁，尚未补齐 `payment-core -> risk-control-system -> 前端人工审核台 -> 再次提交支付` 的整包页面级归档证据。
2. 风控审核员维度的实名账号、权限模型、操作审计和通知机制仍未落地。
3. 真实 MySQL 实库、并发压测和多审核员竞争提交的运行态证据仍缺失。

#### 当前门禁结论

本轮补强提升了 `risk-control-system` 的生产一致性和审核安全性，但仍属于 `test` 分支持续成熟的一部分，不改变当前“禁止 `test -> master`、禁止创建正式 `release/*`”的总体结论。

### 10.46 2026-08-06 gateway-access 启停依赖门禁补强

本轮围绕 `gateway-access` 的启停动作补强依赖门禁，目标是避免治理台账出现“对象本身已不可用，但局部配置仍可被重新启用”的生产风险。

#### 已完成

1. 证书启用前新增依赖校验：已过期证书禁止启用，且证书归属网关必须处于 `ENABLED`。
2. 权限启用前新增依赖校验：归属接入应用未启用时，不允许单独启用权限。
3. 灰度路由启用前新增依赖校验：归属网关未启用时，不允许启用路由，避免把流量切到停用渠道。
4. 单系统设计、自测和接口文档已同步更新，明确上述启用门禁口径。

#### 本轮验证

1. 执行时间：`2026-08-06 10:00 +08:00`
2. 执行命令：

```bash
JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home \
PATH=$JAVA_HOME/bin:$PATH \
/Users/abc123/apache-maven-3.9.16/bin/mvn \
-Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository test
```

3. 执行目录：`systems/gateway-access/backend`
4. 结果：
   - `GatewayAccessControllerTest`：`12 tests, 0 failures, 0 errors, 0 skipped`
   - `GatewayAccessServiceImplTest`：`11 tests, 0 failures, 0 errors, 0 skipped`
   - 模块合计：`23 tests, 0 failures, 0 errors, 0 skipped`

#### 尚未形成门禁证据

1. 仍未覆盖真实第三方网关探活、真实证书轮换、密钥托管和灰度发布审批流。
2. 仍未补齐 `gateway-access -> payment-core / settlement-system` 的真实运行态接入联动证据。
3. MySQL 实库、流量回放和环境切流演练仍未完成。

#### 当前门禁结论

本轮补强提升了 `gateway-access` 的接入治理可靠性，但仍属于 `test` 分支滚动成熟的一部分，不改变当前“禁止 `test -> master`、禁止创建正式 `release/*`”的总体结论。

### 10.47 2026-08-06 ops-config-system 有效配置快照门禁补强

本轮围绕 `ops-config-system` 的有效配置快照补强关键项门禁，目标是避免向 `payment-core`、`risk-control-system` 等下游系统下发缺关键配置的半成品快照。

#### 已完成

1. `effectiveSnapshot` 新增启用状态校验：`businessCode` 对应业务线、`payType` 对应支付类型必须已启用。
2. 新增收银台模板门禁：`terminalType` 未配置启用中的收银台模板时，快照接口直接失败。
3. 新增路由规则门禁：`businessCode + payType` 未命中启用路由规则时，快照接口直接失败。
4. 新增渠道档案门禁：路由规则依赖的主渠道、备选渠道档案若未启用或不存在，快照接口直接失败。
5. 单系统设计、自测和接口文档已同步更新，明确“有效快照只返回可执行配置”这一口径。

#### 本轮验证

1. 执行时间：`2026-08-06 10:10 +08:00`
2. 执行命令：

```bash
JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home \
PATH=$JAVA_HOME/bin:$PATH \
/Users/abc123/apache-maven-3.9.16/bin/mvn \
-Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository test
```

3. 执行目录：`systems/ops-config-system/backend`
4. 结果：`7 tests, 0 failures, 0 errors, 0 skipped`

#### 尚未形成门禁证据

1. 当前仍未覆盖真实配置审批流、配置版本发布、灰度发布时间窗和跨系统配置同步。
2. 当前只证明单系统快照门禁正确，尚未补齐 `ops-config-system + payment-core + risk-control-system` 的整包跨进程回归归档。
3. MySQL 实库、操作审计和配置变更回滚演练仍未完成。

#### 当前门禁结论

本轮补强提升了 `ops-config-system` 对下游系统的配置交付质量，但仍属于 `test` 分支滚动成熟的一部分，不改变当前“禁止 `test -> master`、禁止创建正式 `release/*`”的总体结论。

### 10.40 2026-08-05 payment-core 四端页面级真实回归补证

#### 已完成

1. 当前 HEAD 已新增真实页面级样例覆盖：
   - `SUCCESS`
   - `RISK_REVIEW`
   - `WAIT_CALLBACK`
   - `PREPAY_CREATED`
2. 四端关键页面已形成当前代码基线证据：
   - `app-web`：`SUCCESS`
   - `h5-web`：`RISK_REVIEW`
   - `pc-web`：`WAIT_CALLBACK`
   - `admin-web`：`RISK_REVIEW`
3. 本轮新增两笔真实 `WAIT_CALLBACK` 样例：
   - `PAY1785915556566 / PRE1785915556567`
   - `PAY1785915556767 / PRE1785915556768`
4. 页面证据已明确验证：
   - `SUCCESS` 结果页不再允许模拟回调和关闭支付单
   - `RISK_REVIEW` 结果页不允许模拟回调
   - `WAIT_CALLBACK` 结果页允许主动查单、模拟回调和关闭支付单
   - `admin-web` 支付详情页对动作可用性和处理建议已按状态收敛

#### 尚未形成门禁证据

1. 本轮 `WAIT_CALLBACK` 样例是通过 `sourceAppId=default-app` 创建的，还不是正式来源应用全量闭环证据。
2. 正式来源应用：
   - `housekeeping-app-web`
   - `housekeeping-h5-web`
   - `housekeeping-pc-web`
   当前仍受 `strictMode=开启` 且 `selfCheckStatus=WARN` 约束，提交时会命中 `PAYMENT-1019`。
3. 因此，当前页面级证据只能证明页面状态映射和动作控制逻辑成立，还不能证明正式来源应用已经达到冻结发布口径。

#### 当前门禁结论

1. `payment-core` 已从“只有构建和接口证据”提升到“已有四端真实页面级证据”。
2. 当前仍不能推进 `test -> master`，因为正式来源应用控制策略治理问题仍未收口。
3. 这也意味着下一阶段优先级已经收敛为：
   - 修复正式来源应用控制策略自检告警
   - 用正式来源应用重新补一轮四端页面回归
   - 按新的当前 HEAD 重算预发布检查与发布材料

### 10.39 2026-08-05 payment-core 四端共享层收敛复核

#### 已完成

1. `payment-core/frontend/shared/src` 已新增终端共享层视图与样式入口，开始收敛 `h5-web / pc-web` 对 `app-web` 的直接页面引用。
2. `h5-web` 与 `pc-web` 已补齐共享目录依赖解析配置，最新 `npm run build` 均通过。
3. `payment-core/backend` 已显式切换到 `/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home` 完成 `mvn -DskipTests compile`，证明本轮后端改动未破坏编译。
4. 仓库忽略规则已补齐 `.runtime/`，降低运行时垃圾文件污染 `test` 分支的风险。

#### 尚未形成门禁证据

1. 共享层收敛当前只证明构建通过，尚未形成围绕真实支付单的最新四端页面级人工/自动化回归截图证据。
2. `app-web` 仍是终端共享逻辑的事实来源，后续若要继续提高前端工程独立性，还需把 API、常量和终端视图进一步抽离为真正的 repo-native shared module。
3. 当前仍缺少按本轮 HEAD 重算的正式预发布检查结论和冻结版发布材料。

#### 当前门禁结论

1. 本轮属于“工程治理增强 + 构建回归通过”，可以纳入 `test` 持续演进。
2. 当前仍不满足 `test -> master` 合并条件，也不满足创建正式 release 的门禁标准。

### 10.39 2026-08-04 payment-core 接入 risk-control-system 准入联动

本轮围绕 `payment-core` 提交支付前的独立风控准入链路补强，目标是让支付核心在真正出站到渠道前先接入风控中心决策，而不是只保留本地说明性字段。

#### 已完成

1. `payment-core` 新增 `PaymentRiskControlService`，支持调用 `risk-control-system` 的 `/api/risk-control/decisions/evaluate`。
2. `risk-control-system` 新增支付准入评估接口，支持按黑名单、限额规则、基础高危策略和人工复核结果返回 `PASS / REVIEW / INTERCEPT / REJECT`。
3. `payment-core` 提交支付新增四类风控收口状态：
   - `RISK_REVIEW`
   - `RISK_BLOCKED`
   - `RISK_REJECTED`
   - 正常 `PASS` 后继续原有路由和渠道提交
4. `payment-core` 后端全量自动化测试于 `2026-08-04` 实际执行通过：`263 tests, 0 failures, 0 errors, 0 skipped`。
5. `risk-control-system` 后端自动化测试于 `2026-08-04` 实际执行通过：`5 tests, 0 failures, 0 errors, 0 skipped`。

#### 尚未形成门禁证据

1. 尚未在真实运行态完成 `payment-core` 调用已启动 `risk-control-system` 的跨进程 smoke 证据。
2. 风控复核后的前端收银台页面态、运营后台处理台和消息告警仍缺整包联调证据。

#### 当前门禁结论

本轮把“支付主链路完全未接独立风控系统”这个缺口进一步关闭，但仍不足以触发 `test -> master` 或新的 release。原因不是自动化回归失败，而是跨系统真实运行态、配置生效和整包稳定观察证据仍不足。

#### 当前 HEAD 回归快照

本轮联动补强后，当前工作区新增的有效自动化证据为：

- `payment-core` 后端 `262 tests, 0 failures, 0 errors, 0 skipped`
- `risk-control-system` 后端 `4 tests, 0 failures, 0 errors, 0 skipped`
- 当前整包后端模块的最新报告口径可更新为 `426 tests`

该快照提升了 `test` 分支继续迭代的可信度，但不改变当前“不合并 master、不发布 release”的总门禁结论。

### 10.40 2026-08-05 payment-core 人工复核通过后重提支付门禁补强

本轮围绕 `payment-core` 在“人工复核通过后再次提交同一预付单”这一真实支付断点进行补强，目标是确认风控通过后的支付单不再卡死在 `RISK_REVIEW`。

#### 已完成

1. 修复 `PaymentMapper.xml` 中 `updatePrepayToPaying` 的状态限制，允许 `cashier_status IN ('待支付', '待风控复核')`。
2. 新增 `PaymentServiceImplTest.shouldContinueSubmitAfterRiskReviewApproved`，覆盖 `RISK_REVIEW -> PASS -> 渠道提交`。
3. `PaymentServiceImplTest` 于 `2026-08-05` 实际执行通过：`27 tests, 0 failures, 0 errors, 0 skipped`。
4. `payment-core` 后端全量回归于 `2026-08-05` 实际执行通过：`264 tests, 0 failures, 0 errors, 0 skipped`。
5. 本机真实跨进程 smoke 已拿到以下证据：
   - 预付单 `PRE1785901653980`
   - 支付单 `PAY1785901653979`
   - 复核单 `REVIEW-1785901681130`
   - 渠道流水号 `WX-1785901790893`
   - 首次提交后状态 `RISK_REVIEW / 待风控复核`
   - 人工复核通过后再次提交状态 `WAIT_CALLBACK / 支付中`
6. 主动查单已返回真实最近一次尝试数据：
   - `latestAttemptStatus = 处理中`
   - `latestIdempotencyKey = PRE1785901653980|微信支付|wx_h5`
   - `latestResponsePayload.message = USERPAYING`

#### 尚未形成门禁证据

1. 本轮仅证明“人工复核通过后可继续出站到渠道等待回调”，尚未补齐真实渠道成功回调闭环。
2. 清分、结算、账务在同一支付批次上的联动收口证据仍不足。
3. RabbitMQ 重试、死信、补偿和人工重放虽然已有局部证据，但尚未与本轮大额复核支付单形成整包闭环。
4. 前端四端对“待风控复核 -> 支付中 -> 支付结果页”的整包回归仍需继续补齐。

#### 当前门禁结论

本轮补强后，`payment-core` 已经从“真实链路停在人工复核”前进到“人工复核通过后可以重新回到支付主链路”，显著增强了 `test` 分支的可交付性。

但当前仍未满足以下发布条件：

1. 支付成功回调真实闭环
2. 下游清分/结算/账务联动闭环
3. 前后端整包回归与回滚包证据
4. Release 文档、回滚文档和冻结版门禁清单

因此本轮仍然：

- 不执行 `test -> master`
- 不创建新的 `release/*`
- 继续留在 `test` 分支迭代整包证据

### 10.41 2026-08-05 支付成功回调后跨系统事实闭环复核

本轮围绕 `payment-core` 当前最新真实支付单 `PAY1785901653979`，继续从“人工复核通过后可重新出站”推进到“支付成功回调后下游事实闭环”，目标是确认这不是单体成功，而是支付、清分、账务、结算四系统共同收口。

#### 已完成

1. 启动本地真实运行态：
   - `payment-core`：`18080`
   - `clearing-system`：`18120`
   - `accounting-system`：`18110`
   - `settlement-system`：`18130`
2. 在回调前抓取真实基线：
   - `payment-core` 状态 `WAIT_CALLBACK`
   - `clearing-system` 对 `paymentOrderId=PAY1785901653979` 返回 `0` 条清分记录
   - `accounting-system` `ACT10003` 余额为 `¥12.00`
   - `settlement-system` 尚无本次支付对应的新结算单
3. 对 `PAY1785901653979` 调用 `POST /api/payments/callback/WX_H5` 成功回调后，`payment-core` 真实进入：
   - 支付状态 `SUCCESS`
   - 最近一次支付尝试状态 `成功`
   - 事件轨迹新增 `PAYMENT_SUCCESS | payment.trade.succeeded.v1 | SUCCESS`
4. `clearing-system` 真实生成：
   - 清分单 `CLO20002`
   - 清分批次 `CLB10002`
   - 清分结果 `清分成功`
5. `accounting-system` 真实生成：
   - `PAYMENT_SUCCESS` 事件 `EVT50003`
   - `CLEARING_GENERATED` 事件 `EVT50002`
   - `ACT10003` 新增 `PAYMENT_SUCCESS_EVENT` 流水
   - `ACT10003` 余额由 `¥12.00` 增长至 `¥6900.00`
6. `settlement-system` 真实生成：
   - 结算事件 `SVE70002`
   - 结算单 `SLT20003`
   - 结算批次 `SET10002`
   - 关联清分单 `CLO20002`

#### 尚未形成门禁证据

1. 本轮完成的是 HTTP 直连模式下的真实支付成功闭环，尚未以同一支付单补齐 RabbitMQ 正式拓扑的 retry / DLQ / replay 证据。
2. `SUCCESS` 后的前端四端结果页、后台多台账页面和导出页面尚未以本次支付单做整包回归。
3. 仍缺 release 级数据库迁移回滚、整包发布清单和发布后验证记录。

#### 当前门禁结论

本轮之后，`payment-core` 已从“风控通过后重新进入支付主链路”进一步推进到“支付成功后真实驱动清分、账务、结算三域落地事实”。

这显著提升了当前 `test` 分支的冻结可信度，但仍不足以现在执行：

- `test -> master`
- 基于 `master` 创建新的 `release/*`

当前继续阻断发布的关键缺口已经收敛为：

1. 同批次 MQ 正式链路证据
2. 四端前端整包回归证据
3. 发布包、回滚包和冻结版发布说明

### 10.42 2026-08-05 RabbitMQ 隔离拓扑真实演练补强

本轮围绕 `payment-core` 支付成功事件的 broker 级出站与下游消费补强，目标是把最新一笔支付单从“HTTP 直连闭环”推进到“RabbitMQ 隔离拓扑下的真实事件闭环”。

#### 已完成

1. 本机真实启动 RabbitMQ 3.13 management 容器，并通过 `infra/rabbitmq/docker-compose.yml` 暴露：
   - AMQP `127.0.0.1:5672`
   - 管理端 `127.0.0.1:15672`
2. 通过 `bash infra/rabbitmq/declare_isolated_topology.sh 20260805a` 声明本轮隔离拓扑，覆盖：
   - `clearing.payment-success.20260805a`
   - `accounting.payment-success.20260805a`
   - `settlement.clearing-generated.20260805a`
   - `accounting.clearing-generated.20260805a`
3. 以 AMQP 模式重启：
   - `payment-core`
   - `clearing-system`
   - `accounting-system`
   - `settlement-system`
4. `queue_snapshot.sh 20260805a` 证明 4 个消费者真实在线，主队列、retry、DLQ 初始均无积压。
5. 基于新支付单完成真实支付提交与成功回调：
   - `orderNo = MQ-DRILL-20260805-001`
   - `prepayOrderNo = PRE1785907738213`
   - `paymentOrderId = PAY1785907738212`
6. 首次 `PAYMENT_SUCCESS` 出站时，`payment-core` 因未注入 `HSP_RABBITMQ_PASSWORD` 导致事件 `EVT1785907822039` 进入 `FAILED`，该问题已通过运行态证据暴露，不是推测。
7. 补充 RabbitMQ 凭据重启 `payment-core` 后，执行 `POST /api/payment-events/republish`：
   - `EVT1785907822039` 已由 `FAILED` 变为 `SUCCESS`
8. 重发成功后，下游真实事实已产生：
   - `clearing-system` 生成清分单 `CLO20002`
   - `accounting-system` 生成 `PAYMENT_SUCCESS` 事件 `EVT50002`
   - `settlement-system` 生成 `CLEARING_GENERATED` 事件 `SVE70002`
9. 重发后再次抓取 `queue_snapshot.sh 20260805a`，隔离主队列、retry、DLQ 均无积压，消费者持续在线。

#### 尚未形成门禁证据

1. 本轮证明了 broker 正常投递、消费者在线和失败事件手动重发恢复，但还没有补齐同批次“自动 retry -> DLQ -> intake -> 人工结案 / replay”的完整闭环。
2. 当前仍未用这笔隔离 RabbitMQ 支付单补齐前端四端页面矩阵的最新人工回归。
3. 仍未形成最新版本的正式发布说明、发布后验证清单和冻结版回滚包。

#### 当前门禁结论

本轮之后，“最新支付单在 RabbitMQ 正式拓扑下没有真实运行证据”这一阻断项已显著收敛。

但当前仍不能执行：

- `test -> master`
- `master -> release/*`

原因已经进一步聚焦为：

1. 同批次 retry / DLQ / replay / 人工结案闭环仍未补齐
2. 四端前端围绕真实 MQ 链路的整包人工回归仍未补齐
3. 最新版本发布文档、发布后验证和回滚包仍需按当前 HEAD 重算

### 10.43 2026-08-05 同后缀 DLQ 任务入账、人工结案与定向重放复核

本轮继续沿用 `20260805a` 隔离拓扑，目标是把上一节仍缺失的 `DLQ -> intake -> t_payment_dead_letter_task -> 人工结案 / ready-to-replay / replay -> 下游事实` 同批次补偿链路补齐到当前 HEAD 的真实运行证据。

#### 已完成

1. 通过 RabbitMQ management API 直接向 `payment.trade.dlq` 注入两条演练消息：
   - 人工结案演练消息：
     - `messageId = MQ-DLQ-20260805A-MANUAL-001`
     - `routingKey = payment.success.clearing.dlq.20260805a.v1`
     - `payload = {invalid-manual-json`
   - 定向重放演练消息：
     - `messageId = MQ-DLQ-20260805A-REPLAY-001`
     - `routingKey = payment.success.accounting.dlq.20260805a.v1`
     - `payload` 中包含：
       - `accountNo = ACT10003`
       - `paymentOrderId = PAY-DLQ-20260805A-001`
       - `orderNo = ORD-DLQ-20260805A-001`
       - `amount = 9.99`
2. `payment-core` 已真实消费两条同后缀 DLQ 消息，并创建补偿任务：
   - `DLQb08e16e42e524d739cce`
   - `DLQb8efefb346924a67ac61`
3. 非法 JSON 消息被识别为不可安全重放场景，已通过：
   - `POST /api/payment-dead-letter-tasks/DLQb08e16e42e524d739cce/resolve-manually`
   - 操作人：`支付运营A`
   - 结案备注：`2026-08-05 非法JSON报文，无法安全重放，按隔离演练人工结案`
   - 最终状态：`MANUAL_RESOLVED`
4. 有效重放消息按“人工复核 -> 允许重放 -> 定向重放”顺序收口：
   - `POST /api/payment-dead-letter-tasks/DLQb8efefb346924a67ac61/ready-to-replay`
   - `POST /api/payment-dead-letter-tasks/DLQb8efefb346924a67ac61/replay`
   - 操作人均为 `支付运营A`
   - Ready 备注：`2026-08-05 已核对账户ACT10003存在且报文字段完整，允许定向重放`
   - Replay 备注：`2026-08-05 定向重放至accounting专属replay路由`
   - 最终状态：`REPLAYED`
   - `replayCount = 1`
5. Replay 后已拿到下游真实账务事实：
   - `accounting-system` 新增 `PAYMENT_SUCCESS` 事件 `EVT50004`
   - `bizNo = PAY-DLQ-20260805A-001`
   - 新增账务流水 `LDG20007`
   - `accountNo = ACT10003`
   - `amount = 9.99`
   - `beforeBalance = 200.00`
   - `afterBalance = 209.99`

#### 当前门禁判断

1. 到当前 HEAD 为止，`payment-core` 已不只是“能接住 DLQ 消息”，而是已经在同一隔离后缀 `20260805a` 下拿到：
   - DLQ 入账
   - 人工结案
   - Ready to Replay
   - 定向 Replay
   - Replay 后下游账务落地
   的完整真实证据。
2. 这说明“补偿任务账本是否可用、运营是否可人工处置、Replay 是否真能推动下游恢复”这三类门禁，已经不再停留在文档或单测层。
3. 当前仍然不能推进 `test -> master`，剩余阻断项进一步收敛为：
   - 尚未补一轮“自动 retry -> 自动进入 DLQ”的同批次新证据
   - 四端前端围绕 `RISK_REVIEW / WAIT_CALLBACK / SUCCESS / 多台账回查` 的最新人工回归仍未补齐
   - 正式发布说明、回滚说明、发布后验证清单仍未按当前 HEAD 重算

### 10.44 2026-08-05 fast suffix 自动 retry -> DLQ 真实链路复核

本轮针对 10.43 中仍缺的“自动 retry -> 自动进入 DLQ”门禁，单独声明了一个 `RETRY_TTL_MS=2000` 的 fast suffix 隔离拓扑，并在当前 HEAD 上完成真实演练。

#### 已完成

1. 通过 `RETRY_TTL_MS=2000 bash infra/rabbitmq/declare_isolated_topology.sh 20260805fast` 声明快速重试拓扑：
   - `clearing.payment-success.20260805fast`
   - `accounting.payment-success.20260805fast`
   - 对应 retry / replay / DLQ 路由
2. 演练前队列基线快照显示 fast suffix 主队列、retry、DLQ 均为 `0`，尚无消费者连接。
3. 以独立端口启动 fast consumer：
   - `clearing-system`：`SERVER_PORT=18220`
   - `accounting-system`：`SERVER_PORT=18210`
   - 均显式注入：
     - `HSP_RABBITMQ_USERNAME=hsp`
     - `HSP_RABBITMQ_PASSWORD=hsp-local-drill-20260801`
4. 通过 RabbitMQ management publish API 向 `payment.trade` 发布一条故意无法反序列化的坏报文：
   - `messageId = MQ-FAST-RETRY-DLQ-20260805-001`
   - `routingKey = payment.success.20260805fast.v1`
   - `payload = {invalid-fast-json`
5. 当前 HEAD 下游消费者自动触发失败路由，不依赖人工改任务状态或直接注入 DLQ：
   - `clearing-system` 自动走 `payment.success.clearing.retry.20260805fast.v1`
   - `accounting-system` 自动走 `payment.success.accounting.retry.20260805fast.v1`
6. `payment-core` 的 `payment.compensation.dlq-intake` 活跃消费者真实接住 fast suffix 死信，并生成两条补偿任务：
   - `DLQaba240b93ae346b7b7b9`
   - `DLQ338493c729064115a467`
7. 两条任务都保留了自动重试后的真实死信头：
   - `x-retry-count = 3`
   - `x-first-death-exchange = payment.trade.retry`
   - `x-last-death-reason = expired`
   - `x-death.queue` 分别为：
     - `clearing.payment-success.20260805fast.retry`
     - `accounting.payment-success.20260805fast.retry`
8. 两条 fast suffix 非法报文均已按“不可安全重放”人工结案：
   - clearing：`DLQaba240b93ae346b7b7b9`
   - accounting：`DLQ338493c729064115a467`
   - 统一备注：`2026-08-05 fast suffix 自动重试3次后进入DLQ，报文为非法JSON，保留审计副本并人工结案`
9. 收尾快照显示：
   - fast suffix 主队列、retry 队列均已归零
   - `clearing.payment-success.20260805fast.dlq = 1`
   - `accounting.payment-success.20260805fast.dlq = 1`
   - 说明原始 DLQ 审计副本被保留，未被误删
10. 演练结束后已显式停止 `18220 / 18210` 的临时 fast consumer，未污染长期运行实例。

#### 当前门禁判断

1. 到本轮为止，`payment-core + clearing-system + accounting-system` 已在当前 HEAD 上同时具备：
   - 自动消费失败
   - 自动转入 retry exchange
   - retry TTL 回主队列
   - 达到最大重试次数后自动进入 DLQ
   - DLQ intake 入账补偿任务
   - 运营人工结案
   的整条真实证据。
2. 因此“自动 retry -> 自动进入 DLQ”这一类门禁，已经不再只是历史 `20260803fast` 旧证据，而是被 `20260805fast` 当前 HEAD 再次验证。
3. 当前仍不能推进 `test -> master`，剩余阻断项继续收敛为：
   - 缺少围绕 `RISK_REVIEW / WAIT_CALLBACK / SUCCESS / 多台账页` 的最新四端人工回归证据
   - 缺少按当前 HEAD 重算的正式发布说明、发布后验证清单和回滚文档

### 10.37 2026-08-04 ops-config-system V1 纳入交付检查

本轮新增独立 `ops-config-system`，承接支付协议、业务线、支付类型、收银台模板、渠道档案、路由规则和系统控制的运营配置台账。

#### 已完成

1. 后端查询与启停能力已完成，V1 不再继续依赖 `payment-core` 承担泛配置域职责。
2. 独立 MySQL 数据库脚本、接口文档、后端设计、数据库设计、自测记录已补齐。
3. 单系统后端自动化测试：`3 tests, 0 failures, 0 errors, 0 skipped`。
4. admin-web 生产构建通过。

#### 尚未形成门禁证据

1. 审批流、配置快照、版本治理、灰度发布时间窗尚未落地。
2. 与 `payment-core`、`gateway-access`、`risk-control-system` 的真实配置下发与生效链路尚无运行态证据。
3. MySQL 实库迁移、操作审计和跨系统配置同步未完成。

#### 当前门禁结论

`ops-config-system` 已可纳入 `test` 持续回归，但仍不足以触发 `test -> master` 或新的 release。后续需与风控系统、支付核心和接入治理一起完成真实配置生效链路验证。

### 10.38 2026-08-04 risk-control-system V1 纳入交付检查

本轮新增独立 `risk-control-system`，承接支付风控治理、限额控制和人工复核基线。

#### 已完成

1. 后端完成风险策略、限额规则、黑名单、风险拦截事件、人工复核单和监控规则六类台账。
2. 已完成风险策略、限额规则、黑名单和监控规则启停能力，以及人工复核通过/拒绝动作。
3. 独立 MySQL 数据库脚本、接口文档、后端设计、数据库设计、自测记录已补齐。
4. 单系统后端自动化测试于 `2026-08-04` 实际执行通过：`5 tests, 0 failures, 0 errors, 0 skipped`。
5. `risk-control-system/frontend/admin-web` 于 `2026-08-04` 实际执行 `npm run build` 通过。

#### 尚未形成门禁证据

1. 实时风控引擎、规则发布审批流、评分模型和跨系统事件订阅尚未落地。
2. 与 `payment-core` 的真实支付提交流控/拦截命中链路尚无运行态证据。
3. 消息告警、MySQL 实库迁移、并发压测和生产审计日志未完成。

#### 当前门禁结论

`risk-control-system` 已可纳入 `test` 持续回归，但仍不足以触发 `test -> master` 或新的 release。后续需与 `payment-core`、`ops-config-system` 和 `gateway-access` 一起完成真实命中、生效和复核链路验证。

#### 当前 HEAD 回归快照

本轮风控变更后，当前工作区已具备以下自动化回归证据：

- 后端 `12` 个模块合计 `424 tests, 0 failures, 0 errors, 0 skipped`
- 前端 `15` 个入口具备生产构建产物，包含 `payment-core` 四端、`wallet-system`、`wallet-account`、`gateway-access`、`clearing-system`、`settlement-system`、`accounting-system`、`refund-center`、`reconciliation-system`、`deposit-system`、`ops-config-system`、`risk-control-system`
- 本轮新增真实复验证据为 `risk-control-system` 的 Maven 测试和 admin-web 构建通过
- 当前仍未新增真实 MySQL、RabbitMQ、第三方风控/支付渠道和生产压测证据

该快照只说明自动化回归和前端构建通过，不改变当前“不合并 master、不发布 release”的门禁结论。
