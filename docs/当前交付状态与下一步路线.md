# 当前交付状态与下一步路线

## 1. 文档定位

本文档用于在 `Monday, July 20, 2026` 这个基线时点，说明家政服务支付平台当前已经真实落地的系统、验证结果，以及下一轮继续推进的正式版改造方向。

## 2. 当前已落地系统

### 2.1 payment-core

当前状态：

- 已完成支付交易闭环 V1 核心链路
- 已完成后台工作台、订单中心、支付单管理、支付配置中心、支付监控分析等核心页面
- 已补齐支付协议管理的新增、编辑、启停一体化配置能力
- 已补齐支付协议管理 V1.1，支持协议模板编码/名称、签约要素配置、电子签章服务商维护
- 已补齐支付协议管理 V1.2，支持协议种类字典和协议正文编辑
- 已补齐渠道返回码映射 V1.1，支持映射版本、人工介入判断和归档状态展示
- 已补齐支付渠道管理 V1.1，支持商户应用标识、证书档案、退款时效、验签时间窗和风控标签展示
- 已补齐支付网关接入管理 V1.1，支持环境范围、证书状态、发布阶段、灰度策略、回调白名单和适配器编排展示
- 已补齐支付日终处理 V1.6 页面、接口、批次留痕、成功事实快照和差异告警事实能力
- 已补齐支付日终处理 V1.7 准入口径，支持按主链路事实收口情况统一判断是否可进入正式对账
- 已补齐支付任务中心 V1.6 页面、接口、自动/手动来源留痕、重点告警、任务日志筛选分页和自动补偿调度能力
- 已补齐支付任务中心 V1.7 风险分级，支持按任务类型、失败量和处理规模统一严重等级与升级口径
- 已补齐支付渠道下单适配器 V1，提交支付已切到标准化适配层，不再由 `PaymentServiceImpl` 直接硬编码渠道响应
- 已补齐支付监控 drill-down 能力，支持摘要卡片、异常告警动作、渠道风险提示和明细跳转
- 已补齐支付交易异常中心，支持待回调、回调待处理、事件失败和停用渠道命中的统一排障入口
- 已补齐支付交易异常中心 V1.1，支持批量分派、标记跟进、标记已处理和备注留痕
- 已补齐支付交易异常中心 V1.2，支持 SLA 状态、升级状态和升级建议展示
- 已补齐支付交易异常中心 V1.3，支持异常 SLA 超时自动巡检、任务中心手动补跑和任务日志留痕
- 已补齐支付交易异常中心 V1.4，支持按异常类型识别责任组、责任提示和当前页责任组统计
- 已补齐支付交易异常中心 V1.5，支持当前筛选条件下的后端全量责任组统计，不再依赖前端当前页聚合
- 已补齐支付交易异常中心 V1.6，支持异常 SLA 巡检生成告警 outbox、异常列表展示告警状态、标记已处理时确认告警回执
- 已补齐账单中心、收银台会话、支付流水、统一支付记录的运营筛选与排序增强
- 已补齐支付请求、支付日志、支付事件、支付路由四类运营页面的筛选与排序增强
- 已补齐支付控制管理 V1，提交支付已接入渠道场景匹配、请求渠道有效性校验和单日限额控制
- 已补齐支付控制管理 V1.1，提交支付已进一步接入来源应用支付方式权限、渠道权限、分钟级限流、自检状态阻断和严格模式校验
- 已补齐支付控制管理 V1.2，后台支持执行来源应用自检并回写 `PASS/WARN/FAIL` 准入结果
- 已补齐支付控制管理 V1.3，提交链路新增商户号权限校验与接口访问令牌鉴权，三端收银台已支持运行时透传控制参数
- 已补齐支付控制管理 V1.4，提交链路新增接口提交口径分钟限流，支持按来源应用 + 终端 + 客户端 IP 做运行时拦截
- 已补齐支付回调验签正式化 V1，支持按渠道配置 HMAC-SHA256 / RSA2 算法与密钥材料，回调防重放继续保留
- 已补齐支付提交并发防重 V1.1，提交支付已接入预付单条件占位，避免并发重复下单
- 已补齐真实渠道接入抽象 V1.2，已拆分微信 H5、支付宝 H5、线下银行和本地兜底适配器
- 已补齐退款闭环正式化 V1.8，支持退款详情、退款原因沉淀、退款操作日志沉淀、审核后自动下发本地退款渠道适配器和任务中心失败退款重试统一留痕
- 已完成 `admin-web / app-web / h5-web / pc-web` 四类前端构建复核
- 已完成后端 `89` 个自动化测试通过
- 已完成退款闭环、支付请求管理、支付处理日志管理等核心运营能力的本地正式化版本

当前结论：

- `payment-core` 已是第一阶段最成熟系统
- 但仍不是“完整支付平台冻结版”
- 后续重点应从“继续堆页面”切换为“补正式版工程能力与跨系统联动”

### 2.2 accounting-system

当前状态：

- 已完成 MyBatis + H2 持久化基线
- 已补齐数据库设计、接口文档、后端设计、前端说明
- 后端 `3` 个集成测试已通过
- admin-web 生产构建已通过

当前结论：

- 已具备独立可运行的第一版联调基线
- 后续重点是切换 MySQL、补齐幂等/审计/补偿和跨系统记账联动

### 2.3 clearing-system

当前状态：

- 已完成 MyBatis + H2 持久化基线
- 已补齐清分批次、清分结果、清分规则、费用规则、分账明细、支付成功事件持久化
- 已补齐数据库设计、接口文档、后端设计、前端说明
- 后端集成测试已通过
- admin-web 生产构建已通过

当前结论：

- 已具备独立可运行的第一版联调基线
- 后续重点是切换 MySQL、补齐规则引擎正式化、批次重跑、清分快照、审计追踪和结算联动

### 2.4 settlement-system

当前状态：

- 已完成 MyBatis + H2 持久化基线
- 已补齐结算批次、结算单、审核日志、出款批次、出款记录、清分事件持久化
- 已补齐数据库设计、接口文档、后端设计、前端说明
- 后端集成测试已通过
- admin-web 生产构建已通过

当前结论：

- 已具备独立可运行的第一版联调基线
- 后续重点是切换 MySQL、补齐出款失败重试链路、出款状态回写、回单补偿和审计日志正式化

### 2.5 gateway-access

当前状态：

- 已完成第一版网关接入管理骨架
- 已完成 MyBatis + H2 持久化版本
- 已补齐接入应用、网关渠道、证书、权限与总览页面
- 后端单元测试已通过
- admin-web 生产构建已通过

当前结论：

- 已具备独立可运行的第一版接入治理基线
- 后续重点是切换 MySQL、签名验签、限流、灰度发布和真实渠道适配编排

## 3. 当前 Git 里程碑

本轮关键提交：

1. `a9c2f60` `feat(accounting-system): scaffold accounting admin and services`
2. `9ab02ca` `feat(clearing-system): scaffold clearing admin and services`
3. `98a2a02` `feat(settlement-system): scaffold settlement admin and services`
4. `ff5aaf6` `feat(payment-core): add payment gateway config center`
5. `9f03b80` `feat(gateway-access): scaffold access management`
6. `2e0ab18` `feat(gateway-access): add mybatis persistence baseline`
7. `7c9a652` `chore(platform): refresh build baseline docs`

## 3.1 2026-07-23 最新增量

今天继续补强了 `payment-core` 的支付任务中心与异常告警链路，新增内容包括：

1. 支付交易异常告警派发服务 `PaymentIssueAlertDeliveryService`
2. 本地 IM / SMS / Email 三类告警通知器统一接口
3. 任务中心新增“异常告警派发”手动入口与自动入口
4. `t_payment_issue_alert_log` 增加待派发 outbox 查询与派发状态回写
5. `admin-web` 任务中心页面新增“异常告警派发”操作卡片、文案和前端接口
6. 后端单测 `PaymentTaskCenterServiceImplTest` 扩展到异常告警派发链路，并于 `2026-07-23` 使用本机 JDK `1.8.0_202` 通过 `13` 个测试
7. `admin-web` 于 `2026-07-23` 完成生产构建通过
8. 异常告警派发补齐多通道审计留痕：IM / SMS / EMAIL 每个通道独立写告警日志，原始 outbox 回写 `已派发 / 部分失败 / 派发失败`
9. 后端新增 `PaymentIssueAlertDeliveryServiceImplTest`，并于 `2026-07-23` 与任务中心测试一起通过 `15` 个用例
10. 异常中心列表改为展示 `outbox` 总状态与通道派发摘要，不再被单条通道日志覆盖整体告警状态
11. `PaymentIssueServiceImplTest + PaymentIssueAlertDeliveryServiceImplTest + PaymentTaskCenterServiceImplTest` 于 `2026-07-23` 合计通过 `20` 个用例

这次补齐后的专业判断：

1. `payment-core` 已不再只是“异常 SLA 生成站内 outbox”，而是具备了“outbox -> 本地通知派发骨架 -> 状态回写”的任务中心闭环。
2. 当前派发仍是本地骨架，不是生产级企业微信 / 钉钉 / 短信网关 / 邮件网关直连，也尚未接值班表与升级链路。
3. 因此它提升了冻结版成熟度，但仍不足以支持 `test -> master` 或 `release/*`。

## 4. 下一步正式版优先级

### P0：payment-core 正式版补强

优先补强：

1. 支付协议管理
2. 支付日终处理与对账前置事实收口
3. 支付任务中心与失败重试正式化
4. 真实渠道接入抽象与签名验签正式化
5. 限流、幂等、超时关闭、任务监控正式化
6. 退款中心继续补强为生产版
7. 支付核心与账户账务、清分、结算的跨系统事件边界
8. 真实退款渠道请求、退款验签、退款流水与退款差错补偿正式化

### P1：第二阶段三系统去内存态

优先顺序：

1. `accounting-system` 先落 MyBatis + MySQL
2. `clearing-system` 再落清分规则、批次、明细正式表与 Mapper
3. `settlement-system` 最后落审核、出款、回写正式表与 Mapper

### P2：跨系统联调

目标链路：

1. `payment-core` 支付成功
2. `accounting-system` 记账
3. `clearing-system` 生成清分结果
4. `settlement-system` 生成结算单
5. 出款批次发起

## 5. 当前开发策略

从这个时点开始，后续自动化推进遵循以下原则：

1. 先保证每个子系统有独立边界和第一版可运行骨架
2. 再把最关键的系统逐个从内存态升级到正式持久化实现
3. 每一轮都同时补文档、代码、测试和 Git 提交
4. 优先做最接近真实公司交付包的内容，而不是只做演示页

## 6. 2026-07-22 当日增量判断

### 6.1 payment-core 最新补齐

今天继续补强了 `payment-core` 的支付控制治理，新增内容包括：

1. 支付控制策略批量自检服务
2. 任务中心“支付控制策略自动巡检”手动补跑入口
3. 定时调度自动巡检入口
4. 控制策略告警总览指标
5. 任务日志告警数口径
6. 后台任务中心页面控制策略巡检入口
7. 退款审核后自动下发本地退款渠道适配器

### 6.2 为什么现在还不能合并到 master

截至 `2026-07-22`，`payment-core` 虽然继续向冻结版推进，但仍未达到 `master/release` 门槛，原因仍然是：

1. 已补齐库表口径接口级分钟限流，但网关级 / 多实例分布式限流未完成
2. 真实渠道证书、公钥、多算法签名验签已完成基线化，但仍需要后续对接真实渠道材料与轮换流程
3. 真实退款渠道请求、退款回调验签与退款补偿编排仍未正式化，但退款链路已具备本地渠道适配编排骨架
4. 真实 IM/短信/邮件告警和责任人值班表联动未完成
5. 当前异常告警派发虽已具备本地 IM/SMS/Email 骨架，但尚未接入真实通知供应商、发送结果回执明细与责任人路由编排

### 6.3 发布治理判断

因此当前判断保持不变：

1. 暂不合并 `test -> master`
2. 暂不创建新的 `release/*`
3. 继续在 `feature/payment-core-phase-b` 上补齐 P0/P1 缺口

## 6.4 2026-07-23 晚间补强：值班路由配置化与 DTO 规范统一

本轮继续围绕“冻结版交付包不能只有骨架，还要有一致的工程规范和可配置的责任路由”进行补强，新增内容包括：

1. 支付配置中心总览新增 `异常告警值班路由` 配置块，支持展示路由编码、异常类型、严重等级、责任组、接收人、通知通道、升级等级和班次标签。
2. 后端新增 `t_payment_issue_duty_roster` 初始化数据，沉淀 `待回调未收口 / 回调处理待跟进 / 下游事件发布失败 / 命中停用渠道` 四类异常的默认责任路由。
3. 任务中心超时异常候选查询不再硬编码责任组与接收人，已切换为 `值班路由表优先 + 默认兜底` 的配置化口径。
4. 支付配置中心测试新增值班路由总览与启停覆盖，确认 `PaymentConfigServiceImplTest` 已覆盖该能力。
5. `payment-core` 历史 DTO 继续统一到 `Lombok + 字段中文注释` 标准，`PaymentDetailDTO`、`PaymentRecordRowDTO`、`DashboardSummaryDTO`、`CashierPageDTO`、`PrepayRequestDTO` 等对象已完成收敛。
6. 平台级规范文档已明确：`entity / dto / vo` 统一使用 Lombok，禁止手写基础 `getter/setter`，字段必须带中文语义注释；查询对象允许保留 `getOffset/getLimit` 等派生方法。

本轮验证结果：

1. 后端定向测试于 `2026-07-23` 使用 JDK `1.8.0_202` 与 `/Users/abc123/apache-maven-3.9.16/bin/mvn` 执行，`PaymentTaskCenterServiceImplTest + PaymentConfigServiceImplTest + PaymentIssueServiceImplTest + PaymentIssueAlertDeliveryServiceImplTest` 共 `35` 个用例全部通过。
2. `admin-web` 于 `2026-07-23` 完成生产构建通过，确认值班路由配置区块和前端接口改动未引入构建回归。

当前判断：

1. `payment-core` 的异常告警链路已从“静态责任人”升级为“可配置责任路由 + 本地多通道派发骨架”。
2. 工程规范层面已进一步向冻结版靠拢，但仍未达到真实 `master / release` 发布门槛。
3. 下一步仍应继续补真实通知供应商对接、值班升级链路、跨系统联动和支付核心全量页面/接口缺口。
4. 等满足冻结门槛后，再补发布文档并进入 `master/release` 流程

## 6.5 2026-07-23 夜间补强：值班路由新增/编辑正式化

在上一轮“值班路由配置化”的基础上，这一轮继续把它从“只可查看/启停”补成“配置中心可新增、可编辑”的正式后台能力。

本轮新增内容：

1. 后端新增 `PaymentIssueDutyRosterUpsertRequestDTO` 与 `PaymentIssueDutyRosterEntity`，沉淀值班路由新增/编辑请求和持久化对象。
2. 支付配置中心新增 `POST /api/payment-config/issue-duty-rosters` 与 `PUT /api/payment-config/issue-duty-rosters/{rosterCode}`。
3. `PaymentConfigServiceImpl` 新增值班路由创建、编辑、严重等级校验和编码重复校验逻辑。
4. `PaymentConfigMapper.xml` 新增 `findIssueDutyRosterByCode / insertIssueDutyRoster / updateIssueDutyRoster`。
5. `admin-web` 的“异常告警值班路由”区块新增表单能力，支持后台直接录入和修改责任路由配置，不再只能查看与启停。
6. `PaymentConfigServiceImplTest` 新增值班路由创建、编辑用例。

本轮验证结果：

1. 后端定向测试于 `2026-07-23` 再次执行，`PaymentTaskCenterServiceImplTest + PaymentConfigServiceImplTest + PaymentIssueServiceImplTest + PaymentIssueAlertDeliveryServiceImplTest` 共 `37` 个用例全部通过。
2. `admin-web` 生产构建再次通过，确认值班路由新增/编辑表单未引入构建回归。

当前判断：

1. `payment-core` 的异常值班路由已从“静态展示项”升级为“配置中心可维护项”。
2. 这让系统更像真实公司中的持续迭代支付平台，而不是一次性写死规则的演示系统。
3. 但值班日历、班次切换、升级链路、真实通知供应商对接仍未完成，因此依然不满足 `master / release` 门槛。

## 6.6 2026-07-23 深夜补强：通知通道与升级等级接入派发执行

本轮继续围绕“值班路由里的 `notifyChannels / escalationLevel` 不能只停留在配置表展示，必须真正参与派发执行”的问题进行补强。

本轮新增内容：

1. 异常告警派发项新增 `notifyChannels / escalationLevel / scheduleTag` 字段。
2. 待派发 outbox 查询新增与 `t_payment_issue_duty_roster` 的联动，派发执行阶段可拿到通知通道和升级等级配置。
3. 派发服务从“向所有通知器无差别发送”调整为“仅向值班路由中配置的通道发送”，并自动忽略 `IN_APP` 源通道。
4. 当路由里配置了当前系统暂不支持的通道时，会按失败通道留痕，而不是静默吞掉。
5. 任务日志升级状态文案开始感知最高升级等级，例如 `升级至L2值班负责人 / 纳入L2当班跟进`，不再只输出统一固定文案。

本轮验证结果：

1. 后端定向测试于 `2026-07-23` 使用 JDK `1.8.0_202` 再次执行，`PaymentTaskCenterServiceImplTest + PaymentConfigServiceImplTest + PaymentIssueServiceImplTest + PaymentIssueAlertDeliveryServiceImplTest` 共 `39` 个用例全部通过。
2. `PaymentIssueAlertDeliveryServiceImplTest` 已新增“仅派发配置通道”“不支持通道记失败”两类用例。

当前判断：

1. 当前异常告警链路已经不再是“所有通道一把梭”的演示骨架，而是开始体现“路由配置驱动派发执行”的正式化趋势。
2. 距离生产级通知中心仍有距离，后续还需补真实供应商适配器、发送回执明细、失败重试和供应商级限流。

## 6.7 2026-07-24 上午补强：失败通道补发治理与 DTO 规范收口

本轮继续围绕“异常告警派发不能只支持首发成功，还要支持失败补发、避免重复派发；同时工程对象模型要保持统一可读”的问题进行补强。

本轮新增内容：

1. 待派发 outbox 查询口径从仅处理 `已生成` 扩展为覆盖 `已生成 / 部分失败 / 派发失败`，让失败告警重新进入补发队列。
2. 告警派发执行阶段新增“按 `issueNo + alertChannel` 检查历史成功留痕”的补发保护，避免已成功通道被重复发送。
3. 派发任务文案从单纯“待派发”扩展为“待派发/补发”，让任务中心表达真实运行语义。
4. `PaymentIssueAlertDeliveryServiceImplTest` 新增“补发时跳过已成功通道”覆盖，告警补发编排已具备最小回归保护。
5. `payment-core` 的 `DTO / VO / Entity` 再次执行全量规范体检，`PaymentDetailDTO`、`RefundListItemDTO` 等历史对象已统一补齐类注释与字段中文语义注释。
6. 本轮审计后，`payment-core` 后端对象层 `DTO / VO / Entity` 共 `103` 个类已达到 `Lombok + 字段中文注释` 统一标准，当前自动审计结果为 `0` 个缺口。

本轮验证结果：

1. 后端定向测试于 `2026-07-24` 使用 JDK `1.8.0_202` 与 `/Users/abc123/apache-maven-3.9.16/bin/mvn` 执行，`PaymentIssueAlertDeliveryServiceImplTest + PaymentTaskCenterServiceImplTest + PaymentConfigServiceImplTest + PaymentIssueServiceImplTest + PaymentServiceImplTest` 共 `59` 个用例全部通过。
2. 本轮 DTO/VO/Entity 审计结果为 `TOTAL_PROBLEMS=0`，说明 `payment-core` 对象层已完成这一阶段的统一规范收口。

当前判断：

1. 当前异常告警链路已从“首次派发骨架”进一步升级为“失败告警可补发、成功通道可去重”的更真实任务中心能力。
2. 当前 `payment-core` 对象层可读性和规范性已明显向冻结版交付包靠拢，后续新增对象也应继续保持这一标准。
3. 但真实通知供应商接入、发送回执、补发次数上限、补发间隔与值班升级日历仍未完成，因此仍不满足 `master / release` 门槛。

## 6.8 2026-07-24 中午补强：值班班次生效窗与任务中心联动

本轮继续把“值班路由”从静态配置推进成“班次可生效、任务中心可识别”的配置模块。

本轮新增内容：

1. 异常告警值班路由新增 `effective_start_hour / effective_end_hour`，支持定义班次生效开始/结束小时。
2. 配置中心前端新增班次生效开始/结束小时表单与列表展示，班次不是只写标签，而是可读、可改、可确认的有效时间窗。
3. 任务中心异常升级候选查询按当前小时匹配值班路由，默认班次窗口为 `00:00-23:00`，不破坏现有行为。
4. 异常告警生成内容会附带班次标签与生效时间窗，便于值班同学在任务中心/异常中心直接识别当前责任路由。
5. `PaymentTaskCenterServiceImplTest` 与 `PaymentConfigServiceImplTest` 已补齐相应回归覆盖。

本轮验证结果：

1. 后端定向测试于 `2026-07-24` 使用 JDK `1.8.0_202` 与 `/Users/abc123/apache-maven-3.9.16/bin/mvn` 执行，`PaymentConfigServiceImplTest + PaymentTaskCenterServiceImplTest + PaymentIssueAlertDeliveryServiceImplTest` 共 `35` 个用例全部通过。

当前判断：

1. 当前值班路由已经不再只是“责任人 + 通知通道”的静态展示，而是进一步具备“班次生效窗 + 任务中心按时段匹配”的业务含义。
2. 这让异常告警路由更接近真实企业里的值班配置方式，也更利于后续扩展班次切换与跨班交接。
3. 但值班日历、跨日班次、升级链路和真实外部通知网关仍未完成，因此当前仍然不满足 `master / release` 冻结门槛。
