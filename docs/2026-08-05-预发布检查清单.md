# 2026-08-05 预发布检查清单

## 1. 文档定位

本文档用于描述当前 `test` 分支距离真正 `test -> master -> release/*` 还差哪些门禁，不代表已经允许发布。

当前基线：

- 仓库：`/Users/abc123/workspace/home-service-payment-system`
- 当前分支：`test`
- 当前 HEAD：`cf861a4`
- 当前判断：**仍不允许合并 `master`，更不允许创建 `release/*`**

## 2. 已具备的发布前基础

1. `payment-core` 的支付、回调、清分、结算、账务、DLQ、人工结案和定向重放证据已补齐。
2. `payment-core` 四端构建已通过。
3. `payment-core` 已补齐风控复核、支付成功、MQ 自动 retry -> DLQ 的真实运行态证据。
4. `payment-core` 关键文档已可用于产品、前端、后端、测试联合使用。

## 3. 当前阻断项

### 3.1 四端人工回归

状态：`未完成`

缺口：

1. 需要围绕真实支付单完成四端人工点击确认。
2. 需要把 `RISK_REVIEW / WAIT_CALLBACK / SUCCESS / CLOSED` 的页面行为逐页核对。
3. 需要把 `PENDING_REVIEW / READY_TO_REPLAY / MANUAL_RESOLVED / REPLAYED` 的死信任务页面逐页核对。

参考文档：

- [payment-core 四端人工回归清单](/Users/abc123/workspace/home-service-payment-system/systems/payment-core/docs/payment-core-四端人工回归清单-2026-08-05.md)

### 3.2 发布冻结材料

状态：`未完成`

缺口：

1. 需要把当前 HEAD 重算成正式发布说明。
2. 需要明确当前版本号、发布范围、风险、回滚与发布后验证。
3. 需要在真正决定切 `release/*` 前，再做一版冻结确认。

### 3.3 版本治理

状态：`继续观察`

说明：

1. 当前已经不是“代码没做完”的问题，而是“冻结版发布证据还没收齐”的问题。
2. 即使当前四端构建和多轮联调都已通过，也不能直接把它当成 release 级完成。

## 4. 预发布动作

1. 先完成四端人工回归。
2. 再补正式发布说明草案。
3. 再评估是否允许 `test -> master`。
4. 若 `master` 稳定观察通过，再考虑 `release/*`。

