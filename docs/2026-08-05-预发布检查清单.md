# 2026-08-05 预发布检查清单

## 1. 文档定位

本文档用于描述当前 `test` 分支距离真正 `test -> master -> release/*` 还差哪些门禁，不代表已经允许发布。

当前基线：

- 仓库：`/Users/abc123/workspace/home-service-payment-system`
- 当前分支：`test`
- 当前 HEAD：`e942945`
- 当前判断：**仍不允许合并 `master`，更不允许创建 `release/*`**

## 2. 已具备的发布前基础

1. `payment-core` 的支付、回调、清分、结算、账务、DLQ、人工结案和定向重放证据已补齐。
2. `payment-core` 四端构建已通过。
3. `payment-core` 已补齐风控复核、支付成功、MQ 自动 retry -> DLQ 的真实运行态证据。
4. `payment-core` 关键文档已可用于产品、前端、后端、测试联合使用。
5. 正式来源应用 `housekeeping-app-web / housekeeping-h5-web / housekeeping-pc-web` 已恢复可提交支付，并重新打通到清分、结算、账务下游联动。
6. 四端页面级回归事实已经补到 `SUCCESS / RISK_REVIEW / WAIT_CALLBACK / PREPAY_CREATED` 四类核心状态，不再只有构建和接口证据。

## 3. 当前阻断项

### 3.1 四端人工回归

状态：`部分完成`

缺口：

1. 已有页面级状态事实，但仍缺正式来源应用口径下的页面级截图留痕。
2. 仍需把 `SUCCESS / CLOSED / exception` 组合场景按同批次样例补齐最终人工回归留痕。
3. 仍需把 `PENDING_REVIEW / READY_TO_REPLAY / MANUAL_RESOLVED / REPLAYED` 的死信任务页面补齐可冻结归档证据。

参考文档：

- [payment-core 四端人工回归清单](/Users/abc123/workspace/home-service-payment-system/systems/payment-core/docs/payment-core-四端人工回归清单-2026-08-05.md)

### 3.2 发布冻结材料

状态：`未完成`

缺口：

1. 需要把当前 HEAD `e942945` 重算成冻结版发布说明。
2. 需要明确当前版本号、发布范围、剩余风险、回滚方式与发布后验证清单。
3. 需要在真正决定切 `release/*` 前，再做一版冻结确认和发布门禁复核。

### 3.3 版本治理

状态：`继续观察`

说明：

1. 当前已经不是“代码没做完”的问题，而是“冻结版发布证据还没收齐”的问题。
2. 即使当前四端构建和多轮联调都已通过，也不能直接把它当成 release 级完成。
3. 当前最核心的剩余阻断项已经收敛到：
   - 正式来源应用页面级截图与人工回归归档
   - 同批次补偿链路的冻结版归档整理
   - 基于最新 HEAD 的正式发布说明与发布后验证清单

## 4. 预发布动作

1. 先补正式来源应用口径的页面级截图与人工回归归档。
2. 再补基于当前 HEAD 的正式发布说明草案。
3. 再评估是否允许 `test -> master`。
4. 若 `master` 稳定观察通过，再考虑 `release/*`。
