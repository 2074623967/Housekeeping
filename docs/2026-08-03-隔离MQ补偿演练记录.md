# 2026-08-03 隔离 MQ 补偿演练记录

> 使用范围：`payment-core -> clearing-system -> settlement-system / accounting-system`
> 演练目标：在隔离 RabbitMQ 拓扑 `20260803a` 上，补齐“非法消息进入 DLQ -> payment-core 生成补偿任务 -> 人工结案 / 审核后重放”的真实证据。

## 1. 演练元数据

| 项目 | 记录 |
| --- | --- |
| 日期与时区 | `2026-08-03 13:33 ~ 13:34` `Asia/Shanghai` |
| 执行人 / 复核人 | 执行：Codex；复核：待后续人工复核 |
| 代码版本（本轮新增） | `6f27134`、`bb15d13`，本轮未提交前又补 `payment-core` 隔离 DLQ intake / replay 路由支持 |
| RabbitMQ 版本 / 部署方式 | `rabbitmq:3.13-management`，本地 Docker 容器 `hsp-rabbitmq` |
| vhost / topology | vhost `/`，隔离后缀 `20260803a` |
| 数据库与测试环境 | MySQL 容器 `hsp-payment-mysql-drill`，支付核心库 `housekeeping_payment_core` |
| 关联目的 | 收敛 `test -> master` 剩余阻断项中的 `retry / DLQ / replay / 人工结案` 证据 |

## 2. 业务与消息证据

| 项目 | 记录 |
| --- | --- |
| 注入消息 messageId | `MSG-DRILL-20260803A-001` |
| correlationId | `CORR-DRILL-20260803A-001` |
| 注入 exchange / routing key | `payment.trade` / `payment.success.20260803a.v1` |
| 注入 payload | `{invalid-json` |
| 注入 headers | `x-retry-count=3` |
| clearing 死信任务 | `DLQd45a251d45d6483795bf` |
| accounting 死信任务 | `DLQ8a04f820ab1c414c8750` |
| clearing 死信路由 | `payment.success.clearing.dlq.20260803a.v1` |
| accounting 死信路由 | `payment.success.accounting.dlq.20260803a.v1` |

## 3. 场景记录

| 场景 | 注入方式 | 预期 | 实际结果 | 队列快照 | 数据库 / 接口核验 | 结论 |
| --- | --- | --- | --- | --- | --- | --- |
| 非法消息 / 主数据缺失 | 向 `payment.trade` 发布非法 JSON，且带 `x-retry-count=3`，强制命中 DLQ 分支 | `clearing` 与 `accounting` 分别进入专属 DLQ，并各自生成一条可追溯补偿任务 | 达成 | `2026-08-03 13:33:27 +0800`：`clearing.payment-success.20260803a.dlq=1`、`accounting.payment-success.20260803a.dlq=1` | `GET /api/payment-dead-letter-tasks?messageId=MSG-DRILL-20260803A-001` 返回两条 `PENDING_REVIEW` 任务 | 通过 |
| accounting 人工结案 | `POST /api/payment-dead-letter-tasks/DLQ8a04f820ab1c414c8750/resolve-manually` | 任务转为 `MANUAL_RESOLVED`，保留原因与执行人 | 达成 | 不改变原 DLQ 留痕 | 任务状态变为 `MANUAL_RESOLVED`，`operator=支付运营A`，保留人工说明 | 通过 |
| clearing 审核后定向重放 | 先 `ready-to-replay` 再 `replay` | 任务转为 `REPLAYED`，消息回到 clearing 专属 replay/retry 路径，不串到 accounting | 达成 | `2026-08-03 13:34:05 +0800`：`clearing.payment-success.20260803a.retry=1`，`accounting.payment-success.20260803a.retry=0` | 任务状态变为 `REPLAYED`，`replayCount=1`，`lastReplayAt` 已记录 | 通过 |

## 4. 补偿闭环

| 死信或异常 ID | 原因分类 | 补偿动作 | 执行人 | 执行时间 | 重发 / 冲正结果 | 审计证据 |
| --- | --- | --- | --- | --- | --- | --- |
| `DLQ8a04f820ab1c414c8750` | 非法 JSON，不应进入账务消费 | 人工结案 | `支付运营A` | `2026-08-03 13:34:02 +0800` | 无需重发，保留原 DLQ 与任务台账 | `taskStatus=MANUAL_RESOLVED` |
| `DLQd45a251d45d6483795bf` | 非法 JSON，验证 clearing 重放路径 | 审核后定向重放 | `支付运营A` | `2026-08-03 13:34:02 +0800` | 已成功发送到 `payment.trade.replay / payment.success.clearing.replay.20260803a.v1`，当前停留在 `clearing.payment-success.20260803a.retry`，等待 TTL 回流 | `taskStatus=REPLAYED`、`replayCount=1` |

## 5. 本轮结论

- [x] 非法消息进入专属 DLQ，并生成可追溯补偿任务
- [x] 补偿任务支持人工结案
- [x] 补偿任务支持审核后定向重放
- [x] 隔离后缀路由不会把 replay 串到非目标消费者
- [ ] 本轮尚未等待 `5` 分钟 TTL 到期，因此未补“重放消息从 retry 回流主队列后的再次处理结果”证据
- [ ] 本轮未覆盖 `clearing.generated.*` 链路的非法消息补偿

结论：`有条件通过`

当前这轮已经证明：

1. 隔离 RabbitMQ 拓扑下，`payment-core` 的补偿任务入口可以接住带后缀的隔离 DLQ。
2. `payment-core` 可以把隔离死信解析成可人工操作的补偿任务，并支持 `READY_TO_REPLAY / REPLAYED / MANUAL_RESOLVED` 状态流转。
3. 定向 replay 会落到原消费者专属 replay / retry 路径，不会再广播到其他消费者。

仍需继续补齐：

1. 等待 retry TTL 回流并观察 replay 后的再次处理结果。
2. 以同样方式验证 `clearing.generated.settlement/accounting` 两条链路的非法消息补偿。
3. 把本轮证据并入整包门禁审计，作为 `test -> master` 的最新依据之一。

## 6. 快速 retry 回流补证

由于正式隔离拓扑 `20260803a` 的 retry TTL 是 `5` 分钟，本轮又额外声明了一组快速隔离拓扑：

- suffix：`20260803fast`
- `RETRY_TTL_MS=2000`

### 6.1 payment.success 快速回流验证

| 步骤 | 记录 |
| --- | --- |
| 注入消息 | `messageId=MSG-FAST-PAY-20260803-001`，routing key=`payment.success.20260803fast.v1`，payload=`{invalid-fast-payment-json` |
| `T0` 快照 | `clearing.payment-success.20260803fast.retry=1`、`accounting.payment-success.20260803fast.retry=1` |
| `T+3s` 快照 | 两个 retry 队列仍保有待过期消息 |
| `T+7s` 快照 | 两个 retry 队列已清空，两个专属 DLQ 各为 `1` |
| 补偿任务 | `payment-core` 生成 `DLQb00a3ece218b4b8ca1f4`、`DLQ5688f08734344662afde` 两条新任务 |

关键证据：

1. `headerSnapshot` 中出现 `x-retry-count=3` 与 `x-death.reason=expired`，证明消息并不是第一次失败就进 DLQ，而是经历了 retry TTL 回流后的再次失败。
2. `x-death.queue` 分别指向 `clearing.payment-success.20260803fast.retry` 与 `accounting.payment-success.20260803fast.retry`，说明两条消费者链路仍然保持隔离。

### 6.2 本轮补充结论

1. 到这里，`payment.success.*` 这条链路不只验证了“DLQ 任务生成、人工结案、定向 replay”，也验证了“retry TTL 回流后的再次处理结果最终仍可落入专属 DLQ 并进入补偿任务账本”。
2. 当前剩余需要继续补齐的重点，主要收敛到：
   - `clearing.generated.*` 的快速 retry 回流补证
   - 更多面向正式 release 的整包回归与前端跨端链路证据
