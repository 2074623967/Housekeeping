# 2026-07-31 预发布检查清单

## 1. 文档定位

本文档不是正式 `release/*` 发布说明，也不代表当前允许从 `master` 切版本。

它的用途只有两个：

1. 把当前离正式发布还差哪些门禁写成可执行清单。
2. 为后续真正进入 `master -> release/*` 时复用同一套结构，避免届时从零整理。

当前基线日期：`2026-08-02`
当前目标分支：`master`
当前判断：`test -> master 已完成远端同步；master 已完成可信稳定观察，已创建 v1.0.0 正式发布说明、release 分支和版本标签`

## 2. 当前基线

- 仓库：`/Users/abc123/workspace/home-service-payment-system`
- 当前分支：`master`
- 当前状态：与 `origin/master` 同步
- 重点系统：`payment-core`
- 关联证据：
  - `docs/集成交付门禁审计.md`
  - `docs/整包数据库初始化与回滚演练.md`
  - `systems/payment-core/docs/payment-core自测与联调记录.md`

## 3. 已具备的发布前基础

### 3.1 payment-core 代码与运行时基础

- 四块导出模块已闭环：`bills / cashier-sessions / refunds / payment-flows`
- admin 真实页面点击导出与真实 backend 下载响应已验证
- 原始 `housekeeping_payment_core` 运行库已补齐 `t_payment_task_lease`
- `PAYMENT_ISSUE_ESCALATE` 运行时越界问题已修复
- 原始运行库历史递归升级告警已从 `7` 条 `待确认` 清理为 `0`

### 3.2 payment-core 自动化基础

- `PaymentTaskCenterServiceImplTest`：`17/17`
- `PaymentEventServiceImplTest`：`5/5`
- `PaymentEventDispatchServiceImplTest`：`8/8`
- 当前已具备应用层事件出站边界门禁：
  - 人工重投递语义
  - 失败重试
  - 重试耗尽进 `DEAD_LETTER`
  - 无效事件拒绝
  - 缺详情失败
  - 下游短路保护

### 3.3 整包文档基础

- 已有发布治理规范：`docs/发布门槛与版本治理规范.md`
- 已有正式发布模板：`docs/releases/版本发布说明模板.md`
- 已有数据库初始化与回滚演练记录
- 已有整包门禁审计记录

## 4. 当前阻断项

以下任一项未闭环，都不允许推进 `test -> master`，也不允许创建 `release/*`：

### 4.1 真实 MQ 级可靠投递证据

状态：`已完成`

参考方案：

- `docs/2026-07-31-真实MQ接入与跨系统补偿演练方案.md`

仍缺：

1. 真实消息代理而非本地 HTTP 下游的投递演练
2. 失败重试、重试耗尽、死信落队的真实运行证据
3. 补偿任务对死信/失败事件的处理闭环证据
4. 跨系统消费顺序、幂等和重复投递复核

以上四项已由《集成交付门禁审计》10.17 至 10.22 的真实 RabbitMQ、MySQL 与下游业务事实证据覆盖。

### 4.2 test 分支最终冻结复核

状态：`已完成`

仍缺：

1. 已于审计 10.23 形成明确的拒绝结论和复核口径
2. 已完成 payment / refund / config / issue / task-center 后端冻结复核
3. 已完成 `payment-core -> clearing -> settlement -> accounting` MQ 链路复核
4. 九端前端生产构建已于审计 10.24 通过；稳定观察已完成，审计 10.25 已允许 test -> master

### 4.3 正式发布包内容

状态：`已完成`

`docs/releases/v1.0.0-发布说明.md` 已覆盖版本范围、数据库变更、接口和前端入口、测试结论、风险、回滚及发布后验证。

## 5. 预发布清单

### 5.1 代码冻结前

- `payment-core` 后端定向测试全部通过
- `payment-core` 后端全量测试结果已更新到最新文档
- `payment-core` 前端相关端构建结果已更新到最新文档
- 原始运行库结构迁移与历史数据清理结果已留档
- 整包门禁审计已更新到最新结论

### 5.2 允许推进到 master 前

- 真实 MQ 级可靠投递证据已补齐
- 真实失败重试与死信补偿演练已补齐
- 整包最终冻结复核完成
- 阻断级已知问题为 `0`
- 当前版本范围、风险点、回滚边界已明确

### 5.3 允许创建 release/* 前

- `master` 已稳定观察至少一轮
- 正式发布文档已从模板完整填写
- 数据库变更、接口变更、前端入口变更全部确认
- 发布后验证项能覆盖：
  - 核心支付链路
  - 退款链路
  - 配置链路
  - 异常处理链路
  - 任务中心链路
- 宿主机、RabbitMQ、payment-core 的时间源均与当前 `2026-08-02 Asia/Shanghai` 日期一致，不存在未来日期漂移

## 6. 建议的正式发布文档骨架

真正允许切 `release/*` 后，正式发布说明至少要包括：

1. 版本信息
2. 发布范围
3. 本次功能清单
4. 数据库变更
5. 接口变更
6. 前端变更
7. 测试结论
8. 已知风险
9. 回滚说明
10. 发布后验证项

## 7. 当前结论

截至 `2026-08-02 08:38 +08:00`：

1. 当前可以继续把这份文档当作预发布门禁跟踪件维护。
2. 当前已完成 `v1.0.0` 正式发布说明，后续发布沿用同一模板与门禁结构。
3. `test -> master` 与 `master -> release/v1.0.0` 门禁均已通过；正式生产部署仍须执行版本说明中的环境配置、备份与发布后验证。
