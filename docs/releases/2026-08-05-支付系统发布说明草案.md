# 支付系统发布说明草案

> 状态：草案，不代表当前允许发布

## 1. 版本信息

- 版本号：`draft-e942945`
- 发布日期：`2026-08-06`
- 发布分支：`test`
- 基线分支：`test`

## 2. 发布范围

- 涉及系统：`payment-core`
- 涉及模块：支付提交、收银台、支付结果、风控复核、支付成功事件、清分/账务/结算联动、死信补偿任务
- 不在本次草案范围内：`master`、`release/*`、生产发布

## 3. 本次功能清单

1. 支付结果页补齐 `RISK_REVIEW / WAIT_CALLBACK / RISK_BLOCKED / RISK_REJECTED / PREPAY_CREATED` 状态表达。
2. 收银台补齐提交前状态守卫，避免风控复核或已支付后继续提交。
3. 补齐正式来源应用 `housekeeping-app-web / housekeeping-h5-web / housekeeping-pc-web` 的控制策略恢复、提交流程修复与主链路回归。
4. 补齐 `DLQ -> task -> manual resolve / replay` 真实补偿链路。
5. 补齐 `retry -> DLQ` 快速隔离演练证据。
6. 补齐正式来源应用支付样例到清分、结算、账务的跨系统联动证据。

## 4. 数据库变更

1. 无新增 schema。
2. 仅有运行态补偿任务数据与演练数据落库。

## 5. 接口变更

1. `GET /api/payment-dead-letter-tasks`
2. `POST /api/payment-dead-letter-tasks/{taskNo}/ready-to-replay`
3. `POST /api/payment-dead-letter-tasks/{taskNo}/resolve-manually`
4. `POST /api/payment-dead-letter-tasks/{taskNo}/replay`
5. `POST /api/payments/prepay`
6. `POST /api/payments/submit`
7. `POST /api/payments/callback/{channelCode}`
8. `POST /api/payments/close`

## 6. 前端变更

1. `app-web` 支付结果页状态细化。
2. `app-web` 收银台提交守卫。
3. `h5-web / pc-web` 复用共享视图同步生效。
4. `admin-web` 支付详情页补齐正式来源应用风控复核与支付守卫状态表达。

## 7. 测试结论

1. `app-web` 构建通过。
2. `h5-web` 构建通过。
3. `pc-web` 构建通过。
4. `admin-web` 构建通过。
5. `test` 分支真实 MQ 演练、自动 retry -> DLQ、人工结案与 Replay 闭环已补录。
6. 正式来源应用 App/H5/PC 三端支付样例均已收口为 `SUCCESS`，且下游自动生成清分、结算、账务事实。
7. 正式来源应用成功单重复 `SUCCESS` 回调与显式关闭均不会把成功单错误收口为 `CLOSED`。

## 8. 已知风险

1. 仍缺正式来源应用口径的页面级截图归档，当前只有页面事实记录，尚未形成冻结版截图证据。
2. 仍缺 `SUCCESS / CLOSED / exception` 组合场景的最终人工回归归档包。
3. 仍缺正式 `release/*` 级发布说明、回滚包与发布审批材料。

## 9. 回滚说明

1. 代码回滚方式：若仅回退本轮正式来源应用修复，可基于 `test` 分支按提交粒度回退 `77f5995`、`e3329c3`、`cd9cbec`、`e928485`、`e942945` 对应增量，再重新执行支付主链路与门禁复核。
2. 数据回滚方式：本轮新增主要是演练支付单、清分单、结算单、账务事件与 DLQ 任务数据，不涉及 schema 变更；必要时仅清理演练数据，不影响正式业务库结构。
3. 配置回滚方式：恢复正式来源应用控制策略、终端别名归一化和 RabbitMQ 隔离演练配置到上一稳定基线，并重新校验 `PASS/WARN` 状态。

## 10. 发布后验证项

1. 正式来源应用 App/H5/PC 重新执行一轮 `prepay -> submit -> callback -> success` 主链路验证。
2. 四端页面状态一致性，重点核对 `SUCCESS / RISK_REVIEW / WAIT_CALLBACK / CLOSED`。
3. 风控复核、支付成功、关闭单、死信任务页一致性。
4. MQ 自动 retry / DLQ / intake 证据可复核。
5. 下游清分、结算、账务自动联动事实可复核。
