# 结算出款系统自测记录

## 验证结果

| 项目 | 结果 |
| --- | --- |
| 后端自动化测试 | `3` 个测试通过，`0` failures，`0` errors |
| 后台前端构建 | `npm run build` 通过 |
| 跨系统 smoke | 已消费清分结果事件 `CLO20002`，并在审核通过后自动生成待出款草稿批次 `PBT50001` |

## 当前边界

当前验证覆盖结算单、出款批次、事件消费、审核通过后的待出款草稿生成与后台构建；真实银行出款、回单、出款失败补偿和完整审批编排仍需继续扩展。
