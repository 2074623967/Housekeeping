# 结算出款系统自测记录

## 验证结果

| 项目 | 结果 |
| --- | --- |
| 后端自动化测试 | `5` 个测试通过，`0` failures，`0` errors |
| 后台前端构建 | `npm run build` 通过 |
| 跨系统 smoke | 已消费清分结果事件 `CLO20002`，并完成 `审核通过 -> 待出款草稿批次 -> 执行出款/失败 -> 重试 -> 已出款` 验证 |

## 当前边界

当前验证覆盖结算单、出款批次、事件消费、审核通过后的待出款草稿生成、执行出款、失败重试与后台构建；真实银行出款、回单、自动补偿编排和完整审批流仍需继续扩展。

## 2026-07-31 清分事件重复消费幂等收口

### 验证结果

| 项目 | 结果 |
| --- | --- |
| 定向后端测试 | `SettlementEventServiceImplTest` 共 `2` 个用例通过，`0` failures，`0` errors，`0` skipped |

### 本轮修正

1. `SettlementDataMapper` 新增按 `clearingNo + targetType + targetNo` 查询结算单、按 `eventType + bizNo` 查询结算事件的能力。
2. `SettlementMemoryStore` 在消费 `CLEARING_GENERATED` 前先判断对应结算对象是否已存在结算单，避免重复生成结算单、明细项和结算事件。
3. 新增重复消费单测，确认同一清分事件重复进入后仍只保留 `1` 笔结算单和 `1` 条对应事件台账。

### 当前判断

1. `clearing-system` 对同一清分单的重投或补偿重放，不会再把 `settlement-system` 的结算单重复膨胀。
2. 当前仍未覆盖真实银行出款回执、自动补偿编排、审批流收口和 MQ 级消费确认，因此仍不触发 `test -> master` 或 `release/*`。
