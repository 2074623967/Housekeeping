# wallet-account 阶段 1 候选发布说明

文档日期：2026-08-02

文档状态：候选稿，**尚未实际发布**

适用范围：`feature/wallet-account-v1` 准备进入 `test` 前的子系统冻结说明。

## 1. 版本信息

1. 子系统：`wallet-account`
2. 候选版本：`v0.1.0-rc1`
3. 代码基线：
   - `7095cc3 feat(wallet-account): enforce operation permissions`
   - `9dfcf2c test(wallet-account): verify concurrent idempotent recovery`
4. 当前分支：`feature/wallet-account-v1`

## 2. 本次范围

本次候选版本覆盖钱包账户阶段 1 最小底座能力：

1. 钱包主体建档
2. 钱包开户
3. 账户列表查询
4. 账户详情查询
5. 单账户余额查询
6. 批量余额查询
7. 钱包流水分页查询
8. 钱包流水导出任务受理、任务中心查询与 CSV 下载
9. 钱包账户状态变更

## 3. 代码与页面变更

### 3.1 后端变更

1. 新增 `wallet-account` 后端工程
2. 引入 `dao / mapper / mapper.xml` 分层
3. 增加钱包主体、账户、余额、流水、状态日志、导出任务、幂等记录表
4. 完成开户、查询、流水、导出、状态流转接口
5. 增加权限门禁与请求幂等
6. 增加并发冲突下的业务主键回收逻辑

### 3.2 前端变更

1. 新增后台 `admin-web`
2. 提供钱包账户页
3. 提供钱包流水页
4. 接入真实 API
5. 支持开户、状态切换、流水导出请求、任务中心查询与 CSV 下载

## 4. 数据库变更

本次涉及以下核心表：

1. `t_wallet_owner`
2. `t_wallet_account`
3. `t_wallet_balance`
4. `t_wallet_flow`
5. `t_wallet_account_status_log`
6. `t_wallet_flow_export_task`
7. `t_wallet_idempotent_record`

说明：

1. H2 `schema.sql` 可用于本地演示初始化
2. MySQL 8.0 `database/schema.sql` 已验证可执行
3. 表与字段均已包含中文备注

## 5. 接口变更

本次候选版本主要接口：

1. `GET /api/wallet/accounts`
2. `GET /api/wallet/accounts/{walletAccountNo}`
3. `GET /api/wallet/accounts/{walletAccountNo}/balance`
4. `GET /api/wallet/accounts/balances`
5. `POST /api/wallet/accounts/balances/query`
6. `GET /api/wallet/flows`
7. `POST /api/wallet/flows/export`
8. `POST /api/wallet/accounts`
9. `POST /api/wallet/accounts/{walletAccountNo}/status-change`

## 6. 测试结论

已确认通过：

1. 后端 Maven 测试通过
2. 前端 `npm run build` 通过
3. MySQL DDL 独立验证库执行通过
4. HTTP 主流程联调通过
5. 权限门禁联调通过
6. 真实并发开户压测通过

## 7. 已知风险

1. OpenAPI 与当前实现字段范围尚未完全对齐
2. PRD 中部分筛选项尚未全部落到当前前后端
3. 导出任务已补齐任务中心与下载链路，但后台异步文件归档、下载审计和统一下载中心仍待补齐
4. 页面形态仍偏 MVP，不是完整冻结态后台原型

## 8. 回滚说明

若后续进入 `test` 后发现阻断问题，建议按以下边界回滚：

1. 代码回滚到 `origin/feature/wallet-account-v1` 当前基线 `ed1ce3d`
2. 若需回退本轮并发优化与权限门禁，回滚 `7095cc3`、`9dfcf2c`
3. 若数据库仅新增 wallet-account 独立库表，可整库清理本地测试数据后重建

## 9. 当前结论

本候选说明的作用，是为 `wallet-account` 进入 `test` 做准备，而不是宣告它已经发布。

截至 2026-08-02：

1. 候选版本已经具备较完整的后端与最小前端闭环
2. 建议先合入 `test` 做集成验证
3. 但还不建议直接发版或进入 `master`
