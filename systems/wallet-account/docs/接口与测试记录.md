# wallet-account 接口与测试记录

更新时间：2026-08-02

## 当前接口

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/wallet/accounts` | 账户分页查询，支持 `keyword`、`ownerType`、`accountStatus` |
| `GET` | `/api/wallet/accounts/{walletAccountNo}` | 账户详情与最近流水 |
| `GET` | `/api/wallet/accounts/{walletAccountNo}/balance` | 五类余额查询 |
| `GET` | `/api/wallet/accounts/balances` | 批量余额查询，参数 `walletAccountNos=a,b` |
| `GET` | `/api/wallet/flows` | 流水查询，支持账户号、来源系统、来源业务单号 |
| `POST` | `/api/wallet/accounts` | 幂等开户 |
| `POST` | `/api/wallet/accounts/{walletAccountNo}/status-change` | `INIT -> ACTIVE`、`ACTIVE <-> FROZEN`、零余额 `ACTIVE -> CLOSED` |

## 本轮验证

| 范围 | 命令 | 结果 |
| --- | --- | --- |
| 后端单测 | `mvn -Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository test` | 6 tests passed |
| 前端构建 | `npm_config_cache=/Users/abc123/workspace/home-service-payment-system/.npm-cache npm install --ignore-scripts && npm run build` | passed |
| 差异检查 | `git diff --check` | passed |

## 未通过/未覆盖

1. 前端当前使用演示数据，尚未接真实 API。
2. 尚未执行真实 MySQL 隔离库迁移。
3. 尚未做真实 HTTP 联调、权限校验和并发开户压测。
4. 尚未具备 `test -> master` 所需的独立分支复验、发布说明和回滚演练。
