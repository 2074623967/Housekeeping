# wallet-account 接口与测试记录

更新时间：2026-08-02

## 当前接口

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/wallet/accounts` | 账户分页查询，支持 `keyword`、`ownerType`、`accountStatus` |
| `GET` | `/api/wallet/accounts/{walletAccountNo}` | 账户详情与最近流水 |
| `GET` | `/api/wallet/accounts/{walletAccountNo}/balance` | 五类余额查询 |
| `GET` | `/api/wallet/accounts/balances` | 批量余额查询，参数 `walletAccountNos=a,b` |
| `POST` | `/api/wallet/accounts/balances/query` | 批量余额查询，请求体传账户号列表 |
| `GET` | `/api/wallet/flows` | 流水分页查询，支持账户号、来源系统、来源业务单号 |
| `POST` | `/api/wallet/flows/export` | 异步导出钱包流水，返回导出任务编号 |
| `POST` | `/api/wallet/accounts` | 幂等开户 |
| `POST` | `/api/wallet/accounts/{walletAccountNo}/status-change` | `INIT -> ACTIVE`、`ACTIVE <-> FROZEN`、零余额 `ACTIVE -> CLOSED` |

## 本轮验证

| 范围 | 命令 | 结果 |
| --- | --- | --- |
| 后端单测 | `JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home PATH=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home/bin:$PATH /Users/abc123/apache-maven-3.9.16/bin/mvn -Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository -q clean test` | passed |
| 前端构建 | `npm run build` | passed |
| MySQL DDL | 独立 MySQL 8.0 验证库执行 `database/schema.sql` | passed，6 张核心表创建成功，字段备注可查询 |
| HTTP 联调 | 本地 `8095`：开户、重复开户、详情、状态变更、流水分页查询、导出 | passed，幂等命中，`INIT -> ACTIVE -> FROZEN` 成功，审计操作人编号/名称正确，`total/records` 分页结构符合预期 |
| 并发幂等 | 单测验证 `requestNo` 命中成功结果、账户唯一键冲突回收既有账户 | passed，已补数据库级幂等记录表和成功结果回填 |

## 未通过/未覆盖

1. HTTP 联调已覆盖主流程，尚未覆盖权限校验和真实并发压测。
2. 尚未具备 `feature -> test` 所需的独立分支复验、发布说明和回滚演练。
