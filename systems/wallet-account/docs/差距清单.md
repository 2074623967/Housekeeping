# wallet-account 阶段 1 差距清单

更新时间：2026-08-02

## 已完成

1. 后端 Spring Boot/MyBatis 最小工程可编译运行。
2. H2 演示库 schema/data 可初始化。
3. 开户幂等、账户查询、余额查询、流水查询、状态流转接口已落地。
4. 后端服务层与控制器层共 6 个测试通过。
5. 后台账户与流水页面骨架通过生产构建。

## 下一批

1. 前端接入真实查询和写入接口。
2. 增加主体已存在、账户已存在并发开户的数据库级幂等保护。
3. 增加状态变更权限、审计日志和冻结/解冻独立动作。
4. 补批量余额查询 OpenAPI、导出任务和分页流水接口。
5. 在独立 `feature/wallet-account-phase-1` 分支完成提测，随后进入 `test`。

## 当前门禁判断

当前仅达到“本地最小骨架可运行”，尚未达到 `feature -> test` 门槛，因为前端未真实联调，OpenAPI/PRD/正式 DDL 包尚未完整收进仓库，且没有隔离 MySQL 与真实 HTTP 证据。
