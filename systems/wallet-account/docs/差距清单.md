# wallet-account 阶段 1 差距清单

更新时间：2026-08-02

## 已完成

1. 后端 Spring Boot/MyBatis 最小工程可编译运行。
2. H2 演示库 schema/data 可初始化。
3. 开户幂等、账户查询、余额查询、流水查询、流水导出、状态流转接口已落地。
4. 状态日志表、余额表与 `dao` 分层已补齐，服务编排不再直连 mapper。
5. 后端服务层与控制器层测试通过。
6. 后台账户与流水页面已接入真实 API，支持开户、状态变更与导出动作，并通过生产构建。

## 下一批

1. 增加状态变更权限、关闭账户二次校验提示和审计字段补强。
2. 补真实权限校验和并发开户压测。
3. 对齐 OpenAPI/PRD 与当前实现差异，补齐导出任务中心衔接。
4. 在独立 `feature/wallet-account-v1` 分支完成提测门禁复验，随后再判断是否进入 `test`。

## 当前门禁判断

当前已达到“真实 MySQL 可建表、本地可联调、数据库级幂等已落地”的阶段 1 水平，但尚未达到 `feature -> test` 门槛，因为缺少权限校验证据、真实并发压测和提测门禁审计。
