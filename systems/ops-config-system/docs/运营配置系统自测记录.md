# 运营配置系统自测记录

## 执行环境

- 日期：2026-08-06
- JDK：`1.8.0_202`
- Maven：`/Users/abc123/apache-maven-3.9.16/bin/mvn`
- 数据库：H2 MySQL 模式

## 后端测试

```bash
JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home \
PATH=$JAVA_HOME/bin:$PATH \
/Users/abc123/apache-maven-3.9.16/bin/mvn -q \
-Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository test
```

结果：`7 tests, 0 failures, 0 errors, 0 skipped`。

## 覆盖范围

- 总览和各类配置台账查询
- 路由规则启停
- 空配置编码错误校验
- 有效配置快照查询，包括默认支付方式、优先渠道和系统控制项装配
- 有效配置快照缺失业务线、收银台模板、路由规则时的失败前置校验

## 跨系统联动验证

2026-08-04 已完成一轮 `ops-config-system -> payment-core` 定向联动回归，验证口径如下：

1. `payment-core` 在提交支付前调用 `GET /api/ops-config/effective-snapshot`
2. 当前端未传 `paymentMethod` 时，能够自动补齐 `微信支付`
3. 当前端未传 `channelCode` 时，能够自动补齐 `CHANNEL_WX_H5`
4. 当前联动为支付核心真实业务调用，不是仅靠测试桩覆盖

对应测试证据：

- `ops-config-system`：`mvn test`，`7 tests, 0 failures, 0 errors, 0 skipped`
- `payment-core`：`mvn -Dtest=PaymentServiceImplTest test`，`26 tests, 0 failures, 0 errors, 0 skipped`

## 未验证边界

尚未验证真实 MySQL 迁移、审批流、配置快照、灰度发布和跨系统配置下发；这些能力不应由当前 H2 自测替代。
