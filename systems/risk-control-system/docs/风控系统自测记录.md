# 风控系统自测记录

## 执行环境

- 日期：2026-08-06
- JDK：`1.8.0_202`
- Maven：`/Users/abc123/apache-maven-3.9.16/bin/mvn`
- 数据库：H2 MySQL 模式

## 后端测试

```bash
JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.8.0_202.jdk/Contents/Home \
PATH=$JAVA_HOME/bin:$PATH \
/Users/abc123/apache-maven-3.9.16/bin/mvn -q \
-Dmaven.repo.local=/Users/abc123/apache-maven-3.9.16/repository test
```

结果：`6 tests, 0 failures, 0 errors, 0 skipped`。

## 覆盖范围

- 总览、策略、限额和复核单查询
- 人工复核通过动作
- 人工复核单重复审核拦截，验证已通过复核单不能再次执行审核动作
- 空策略编码错误校验
- 支付准入评估命中手机号黑名单后生成复核单，并在人工审核通过后放行
- 支付准入评估读取 `ops-config-system` 的 `CONTROL_BIG_AMOUNT_REVIEW` 控制项后，对大额交易自动转人工复核

## 未验证边界

尚未验证实时风控引擎、跨系统事件订阅、消息告警、MySQL 实库迁移和高并发压测；这些能力不能由当前 H2 自测替代。

## 运行态补充验证

2026-08-04 已完成一轮跨进程 smoke：

1. `ops-config-system` 运行态开启 `CONTROL_BIG_AMOUNT_REVIEW`
2. `risk-control-system` 运行态临时关闭本地 `LIMIT_USER_DAY`，隔离本地限额规则影响
3. 直接调用 `POST /api/risk-control/decisions/evaluate`
4. 业务单号 `PAY-RUNTIME-SMOKE-20260804-002` 命中 `大额支付人工复核`
5. 生成复核单 `REVIEW-1785811212924`

这证明当前风控系统已经不只是本地种子规则决策，还能真实消费 `ops-config-system` 的系统控制项。
