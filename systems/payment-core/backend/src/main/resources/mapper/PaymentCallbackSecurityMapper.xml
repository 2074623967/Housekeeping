<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc123.hsp.mapper.PaymentCallbackSecurityMapper">

    <select id="findCallbackSecretByChannelCode" resultType="string">
        SELECT callback_secret
        FROM t_payment_channel_config
        WHERE LOWER(channel_code) = LOWER(#{channelCode})
        LIMIT 1
    </select>

    <select id="findCallbackSecurityProfileByChannelCode" resultType="com.abc123.hsp.dto.PaymentCallbackSecurityProfileDTO">
        SELECT LOWER(channel_code) AS channelCode,
               callback_sign_algorithm AS callbackSignAlgorithm,
               callback_secret AS callbackSecret,
               callback_public_key AS callbackPublicKey,
               callback_notify_url AS callbackNotifyUrl,
               notify_sign_window_sec AS notifySignWindowSec
        FROM t_payment_channel_config
        WHERE LOWER(channel_code) = LOWER(#{channelCode})
        LIMIT 1
    </select>

    <insert id="insertCallbackNonce">
        INSERT INTO t_payment_callback_nonce(channel_code, nonce, payment_order_id, expires_at, created_at)
        VALUES (LOWER(#{channelCode}), #{nonce}, #{paymentOrderId}, DATE_ADD(NOW(), INTERVAL #{expireSeconds} SECOND), NOW())
    </insert>

    <delete id="deleteExpiredNonce">
        DELETE FROM t_payment_callback_nonce
        WHERE expires_at &lt;= NOW()
    </delete>
</mapper>
