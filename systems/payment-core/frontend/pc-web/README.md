# pc-web

## 模块定位

这是 `payment-core` 的 PC 用户支付端，面向：

- 官网支付页
- 桌面浏览器支付页
- 后台代客支付或客服辅助支付场景

## 当前承接范围

- PC 收银台
- PC 支付结果页
- 与 `app-web / h5-web` 同口径的支付交易闭环联调

## 设计原则

1. 交易逻辑与 `app-web / h5-web` 保持一致。
2. 终端文案、视觉层和操作提示保持 PC 端独立表达。
3. 后续若引入二维码、桌面轮询和更多失败补救动作，优先在本端扩展。
