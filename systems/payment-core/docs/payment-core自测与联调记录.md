# payment-core 自测与联调记录

## 1. 记录目的

本文档用于沉淀 `payment-core` 当前冻结版 V1 的实际验证情况，明确：

1. 已验证项
2. 未完全验证项
3. 受环境限制项
4. 当前风险

## 2. 已完成验证

### 2.1 前端构建验证

已完成：

1. `systems/payment-core/frontend/admin-web` 可完成 Vite 构建
2. `systems/payment-core/frontend/app-web` 可完成 Vite 构建

结论：

- 当前前端代码至少满足静态构建通过标准

### 2.2 数据库验证

已完成：

1. 用户态 MySQL 实例启动成功
2. 库 `housekeeping_payment_core` 创建成功
3. `schema.sql` 建表成功
4. 表数量校验为 `11` 张
5. `t_payment_order` 存量样例数据存在

结论：

- 当前 DDL 可在 MySQL 中落地执行

### 2.3 文档与代码对齐验证

已完成：

1. 后端分层结构与规范一致
2. `application.yml` 已切至独立数据库
3. `schema.sql` 已补表注释与字段注释
4. `entity` 已统一使用 `@Data`
5. 前端页面与主链路接口已形成对应关系

## 3. 未完全验证项

### 3.1 后端完整编译

当前状态：

- Maven 本地仍受依赖拉取与环境限制影响，尚未完成一轮完全无阻塞的 `mvn compile/test`

影响：

- 需要后续在完整 Maven 环境下补一次标准编译与接口启动验证

### 3.2 真实接口联调

当前状态：

- 文档、数据库、前端构建已验证
- 但最新后端代码尚未在完整 Spring Boot 运行态下做一轮全链路联调证据沉淀

影响：

- 需补接口实际调用记录、返回报文记录、数据库状态变化截图或日志

## 4. 当前风险清单

1. 查询筛选逻辑尚未全部下推到后端 SQL，当前仍存在部分前端过滤。
2. 回调验签、防重、冲突收敛尚未达到生产级标准。
3. 主动查单当前查的是本地详情，不是真实渠道。
4. 关闭支付单与回调晚到冲突场景尚需后续增强验证。

## 5. 当前结论

从工程交付角度看，`payment-core` 当前已经达到：

1. 结构规范可审查
2. 页面与接口可开发
3. 数据库可落地
4. 文档可交付

从生产投产角度看，当前还需要继续补：

1. 完整编译运行验证
2. 后端接口自测证据
3. 真实渠道与补偿能力
