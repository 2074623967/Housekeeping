# 清分清算系统自测记录

## 验证结果

| 项目 | 结果 |
| --- | --- |
| 后端自动化测试 | `5` 个测试通过，`0` failures，`0` errors |
| 后台前端构建 | `npm run build` 通过 |
| 跨系统 smoke | 已消费支付成功事件 `PAY1785289709999`，并自动向结算、账务派发清分结果 |

## 当前边界

当前验证覆盖清分规则、批次、支付成功事件消费、清分结果自动派发、后台构建与联调；真实渠道对账文件、MQ 自动投递、失败重试与死信补偿留待后续迭代。

## 2026-07-31 支付成功重复消费幂等收口

### 验证结果

| 项目 | 结果 |
| --- | --- |
| 定向后端测试 | `ClearingEventServiceImplTest,ClearingEventDispatchServiceImplTest` 共 `4` 个用例通过，`0` failures，`0` errors，`0` skipped |

### 本轮修正

1. `ClearingMemoryStore` 新增按 `paymentOrderId` 查询既有清分单与支付成功事件的幂等判断，避免同一支付成功事件被重复消费时再次生成清分单、分账项和本地事件。
2. 保留清分结果下游派发能力不变，因此这次收口的是“本地清分工件不重复生成”，不是“真实 MQ 已补齐”。
3. 新增重复消费单测，确认同一 `paymentOrderId` 连续两次消费后仍只保留 `1` 笔清分单、`1` 条对应支付成功事件和 `3` 条新增分账项。

### 当前判断

1. `payment-core` 重投递再次命中 `clearing-system` 时，不会再把本地清分单、分账项和事件台账重复膨胀。
2. 当前仍未覆盖真实消息总线、消费回执、失败重试、死信补偿与下游系统生产级幂等，因此仍不触发 `test -> master` 或 `release/*`。
